# خطة بناء المصمم (أغلفة يوتيوب وريلز وتيك توك)

> **للوكلاء المنفذين:** سكيل مطلوب: `subagent-driven-development` (صاحب القناة اختار "فريق وكلاء") حتى تنفذ الخطة مهمة بمهمة. الخطوات بصيغة (`- [ ]`) للمتابعة.
>
> **المهام 1-7:** وكلاء. **المهمتين 8 و9:** يسويهن مدير الإنتاج (الجلسة الرئيسية) بنفسه، لأن يحتاجن أدوات Canva وموافقة صاحب القناة، والوكيل ما يگدر يسأله.

**الهدف:** مصمم يطلع 4 أغلفة يوتيوب سينمائية من فكرة أو حلقة (المقدم داخل المشهد، واللغز يسار، وكتابة سطرين مايلة)، وصاحب القناة يختار، وبعدها يسوي غلاف ريلز وتيك توك وشورتس، بستايل الأغلفة الي اختارها صاحب القناة.

**البناء:**
- Canva (عن طريق أدوات MCP) يولّد **المشهد كامل والمقدم داخله**، وصورة وجهه تنحط كمرجع. الصور تنزل عن طريق "لوحات تنزيل" ثابتة.
- الكود بـ Python يركّب الغلاف من ملف طبقات JSON:
  - يكتب العربي بخطوط حقيقية (Pillow مع raqm).
  - يدعم التدرج والميلان ورقعة الورق واللمسة السينمائية.
- سكيل `designer` يربط كلشي خطوة بخطوة، وموظف `designer` بالاستوديو يشتغل بعد `packager`.

**التقنيات:** Python 3.11، وPillow 12 (بيه raqm وfribidi جاهزين)، وarabic-reshaper، وpython-bidi، وrembg (وقت الحاجة بس)، وunittest، وأدوات Canva MCP.

**ملف التصميم:** `docs/superpowers/specs/2026-09-25-designer-design.md`.

**ملاحظة:** كل الكود هنا انجرّب بمجلد تجارب قبل ما تنكتب الخطة: 53 اختبار نجحت، وستايل الكتابة طلع مطابق لأغلفة صاحب القناة، ومشهد Canva التجريبي طلع احترافي.

## القيود العامة
- **المقاسات:** يوتيوب 1280×720، وطولي 1080×1920. غلاف يوتيوب النهائي `.jpg` وأقل من 2MB.
- **الخطوط:** `Baloo` (Baloo Bhaijaan 2 ExtraBold، الأساسي)، و`Rubik` (Black)، و`Cairo` (Black)، و`Lalezar`. كلها رخصتها OFL.
- **الكتابة العربية:** يكتبها الكود بس. ولا مرة يكتبها الذكاء الاصطناعي داخل الصورة.
- **حدود الغلاف:** 4 كلمات بالأكثر، و3 عناصر بالأكثر.
- **الخصوصية:** `designer/faces/` و`designer/references/` ما ينرفعن للريبو أبداً، لأن الريبو عام.
- **الفلوس:** ماكو خدمات مدفوعة. Canva ضمن اشتراكه، وvidIQ للتقييم بس، بعد النشر، وإذا الرصيد 30 أو أكثر.
- **لوحات التنزيل:** صاحب القناة طلب ينسأل قبل كل حفظ. فنجمع صور الجولة كلها ونسأله **مرة وحدة** قبل الحفظ. وأي تصميم ثاني إله يحتاج موافقته هم.
- **اللغة:** كل كلام يوصل لصاحب القناة بالعراقي وبدون إنگليزي. أسماء المتغيرات والدوال بالإنگليزي.
- **الـ commit:** كل رسالة commit تنتهي بسطري:
  ```
  Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_017qN4NgwwvAUrzDHBs6DG77
  ```

## نقاط المراجعة (أكثر شي ممكن يأذي صاحب القناة)
1. **سطر عربي بيه أرقام أو إنگليزي** ("300 مليار $"): الكلمات لازم تترتب صح من اليمين. ← اختبار `test_mixed_numbers_and_arabic` (المهمة 1).
2. **عنوان طويل بخط كبير يطلع برا الصورة:** لازم تحذير، مو صورة مقصوصة بصمت. ← `test_text_overflowing_canvas_warns` (المهمة 2).
3. **صورة وجه بدون شفافية مع حد أبيض:** الحد يطلع مربع. لازم تحذير. ← `test_opaque_face_with_outline_warns` (المهمة 2).
4. **غلاف يوتيوب محفوظ PNG وحجمه أكثر من 2MB:** يوتيوب يرفضه. لازم تحذير، و`.jpg` يطلع أقل. ← `test_cli_warns_when_png_too_big_for_youtube` و`test_cli_saves_jpg_under_2mb` (المهمة 2).
5. **عنوان مايل بخط Baloo قريب من الزاوية:** التحذير لازم ينحسب من الحبر الحقيقي بعد الميلان، مو من فراغ الخط، حتى ما يطلع تحذير كاذب ولا يضيع تحذير صحيح. ← `test_brand_headline_template_has_no_false_warning` و`test_rotated_text_still_warns_in_unsafe_zone` و`test_text_bbox_is_real_ink` (المهمة 2).

---

### المهمة 1: الأساس (المكتبات والخطوط والكتابة العربية)

**الملفات:**
- إنشاء:
  - `designer/requirements.txt`
  - `designer/fonts/`: أربع خطوط (`BalooBhaijaan2-ExtraBold.ttf` و`Rubik-Black.ttf` و`Cairo-Black.ttf` و`Lalezar-Regular.ttf`) ورخصها (`OFL-*.txt`)
  - `designer/arabic.py`
  - `designer/tests/test_arabic.py`
- تعديل: `.gitignore` و`.claude/settings.json`

**الواجهات:**
- تنتج:
  - `arabic.RAQM: bool`
  - `arabic.is_rtl(text: str) -> bool`
  - `arabic.load_font(path, px: int) -> ImageFont.FreeTypeFont`
  - `arabic.shape_word(word: str) -> tuple[str, dict]`: يرجع النص ومعاملات `draw.text`.
  - `arabic.visual_words(line: str) -> list[str]`: الكلمات بترتيب الرسم من اليسار لليمين.

- [ ] **الخطوة 1: المكتبات**

`designer/requirements.txt`:
```
pillow>=12.0
arabic-reshaper>=3.0
python-bidi>=0.6
```
شغّل:
```bash
pip install -q -r designer/requirements.txt
python3 -c "from PIL import features; print(features.check('raqm'))"
```
المتوقع: `True`

- [ ] **الخطوة 2: الخطوط العربية ورخصها**
```bash
mkdir -p designer/fonts
curl -sS -o designer/fonts/BalooBhaijaan2-ExtraBold.ttf https://fonts.gstatic.com/s/baloobhaijaan2/v21/zYXwKUwuEqdVGqM8tPDdAA_Y-_bMKo1EhQd2tWxo8TwRTaP4.ttf
curl -sS -o designer/fonts/Rubik-Black.ttf https://fonts.gstatic.com/s/rubik/v31/iJWZBXyIfDnIV5PNhY1KTN7Z-Yh-ro-1UA.ttf
curl -sS -o designer/fonts/Cairo-Black.ttf https://fonts.gstatic.com/s/cairo/v31/SLXgc1nY6HkvangtZmpQdkhzfH5lkSs2SgRjCAGMQ1z0hEk5W1Q.ttf
curl -sS -o designer/fonts/Lalezar-Regular.ttf https://fonts.gstatic.com/s/lalezar/v16/zrfl0HLVx-HwTP82UaA.ttf
for f in baloobhaijaan2:BalooBhaijaan2 rubik:Rubik cairo:Cairo lalezar:Lalezar; do
  curl -sS -o designer/fonts/OFL-${f#*:}.txt https://raw.githubusercontent.com/google/fonts/main/ofl/${f%%:*}/OFL.txt
done
file designer/fonts/*.ttf
```
المتوقع: الأربعة `TrueType Font data`.
- إذا رابط الخط تغيّر، جيب الرابط الجديد من:
  ```bash
  curl -s "https://fonts.googleapis.com/css2?family=Baloo+Bhaijaan+2:wght@800&family=Rubik:wght@900&family=Cairo:wght@900&family=Lalezar" -A "Mozilla/4.0"
  ```

- [ ] **الخطوة 3: اكتب الاختبار الفاشل** `designer/tests/test_arabic.py`
```python
import sys
import unittest
from pathlib import Path
from unittest import mock

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
import arabic  # noqa: E402

FONT = Path(__file__).resolve().parents[1] / "fonts" / "Lalezar-Regular.ttf"


class ArabicTest(unittest.TestCase):
    def test_is_rtl(self):
        self.assertTrue(arabic.is_rtl("فلوس"))
        self.assertFalse(arabic.is_rtl("iPhone 17"))

    def test_arabic_line_words_reversed_for_drawing(self):
        self.assertEqual(arabic.visual_words("فلوس الناس وين"), ["وين", "الناس", "فلوس"])

    def test_english_line_keeps_order(self):
        self.assertEqual(arabic.visual_words("iPhone 17 Pro"), ["iPhone", "17", "Pro"])

    def test_mixed_numbers_and_arabic(self):
        self.assertEqual(arabic.visual_words("300 مليار $"), ["$", "مليار", "300"])

    def test_raqm_available_in_pillow(self):
        self.assertTrue(arabic.RAQM, "Pillow بدون raqm: الحروف العربية راح تطلع مقطعة")

    def test_shape_word_arabic_uses_rtl_with_raqm(self):
        with mock.patch.object(arabic, "RAQM", True):
            self.assertEqual(arabic.shape_word("فلوس"), ("فلوس", {"direction": "rtl", "language": "ar"}))

    def test_shape_word_latin_untouched(self):
        self.assertEqual(arabic.shape_word("300"), ("300", {}))

    def test_fallback_without_raqm_connects_letters(self):
        with mock.patch.object(arabic, "RAQM", False):
            text, kw = arabic.shape_word("فلوس")
        self.assertEqual(kw, {})
        self.assertNotEqual(text, "فلوس")  # صارت أشكال حروف متصلة ومعكوسة للرسم

    def test_load_font(self):
        font = arabic.load_font(FONT, 40)
        self.assertGreater(font.getlength("فلوس", direction="rtl"), 0)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **الخطوة 4: شغّله وتأكد إنه يفشل**

شغّل: `python3 -m unittest discover -s designer/tests -v`
المتوقع: فشل بـ `ModuleNotFoundError: No module named 'arabic'`

- [ ] **الخطوة 5: اكتب** `designer/arabic.py`
```python
"""تجهيز الكتابة العربية للرسم: كل كلمة تنرسم لحالها، والكلمات تترتب من اليمين لليسار."""
import re

from PIL import ImageFont, features

RAQM = features.check("raqm")
ARABIC = re.compile(r"[؀-ۿݐ-ݿࢠ-ࣿﭐ-﷿ﹰ-﻿]")


def is_rtl(text: str) -> bool:
    return bool(ARABIC.search(text))


def load_font(path, px: int) -> ImageFont.FreeTypeFont:
    engine = ImageFont.Layout.RAQM if RAQM else ImageFont.Layout.BASIC
    return ImageFont.truetype(str(path), px, layout_engine=engine)


def shape_word(word: str) -> tuple[str, dict]:
    """يرجع النص الجاهز للرسم ومعاملات draw.text الإضافية."""
    if not is_rtl(word):
        return word, {}
    if RAQM:
        return word, {"direction": "rtl", "language": "ar"}
    import arabic_reshaper
    from bidi.algorithm import get_display
    return get_display(arabic_reshaper.reshape(word)), {}


def visual_words(line: str) -> list[str]:
    """ترتيب الكلمات مثل ما تنرسم من اليسار لليمين."""
    words = line.split()
    return list(reversed(words)) if is_rtl(line) else words
```

- [ ] **الخطوة 6: شغّل الاختبارات**

شغّل: `python3 -m unittest discover -s designer/tests -v`
المتوقع: 9 اختبارات `OK`

- [ ] **الخطوة 7: خلي git يتجاهل صور الوجه والمراجع**

ضيف بآخر `.gitignore`:
```
# المصمم: صور الوجه والمراجع ما تنرفع (الريبو عام)
designer/faces/
designer/references/
__pycache__/
```
تأكد: `git check-ignore designer/faces/x.png designer/references/y.jpg`، والمتوقع يطبع المسارين.

- [ ] **الخطوة 8: تثبيت المكتبات تلقائياً ببداية كل جلسة**

بـ `.claude/settings.json`، ضيف أمر ثاني داخل نفس مصفوفة `hooks`، بعد أمر `update-skills.sh`:
```json
{
  "type": "command",
  "command": "pip install -q -r \"$CLAUDE_PROJECT_DIR\"/designer/requirements.txt >/dev/null 2>&1 || true",
  "timeout": 180
}
```
تأكد إن الملف JSON سليم: `python3 -m json.tool .claude/settings.json >/dev/null && echo ok`

- [ ] **الخطوة 9: Commit**
```bash
git add designer/requirements.txt designer/fonts designer/arabic.py designer/tests/test_arabic.py .gitignore .claude/settings.json
git commit -m "Add designer foundation: Arabic text shaping, fonts, requirements"
```

---

### المهمة 2: المركّب (`render.py`)

**الملفات:**
- إنشاء:
  - `designer/render.py`
  - `designer/tests/test_render.py`
  - `designer/examples/sample-youtube.json`

**الواجهات:**
- تستخدم من المهمة 1: `load_font` و`shape_word` و`visual_words`.
- تنتج:
  - `render.SpecError(ValueError)`
  - `render.load_spec(path) -> dict`
  - `render.size_name(spec) -> str | None`
  - `render.canvas_size(spec) -> tuple[int, int]`
  - `render.render(spec: dict, base_dir) -> tuple[Image.Image, list[str]]`: الصورة والتحذيرات بالعربي.
  - `render.draw_guides(img, name) -> Image.Image`
  - `render.save(img, out: Path) -> None`
  - `render.main(argv) -> int`: صفر إذا نجح، و1 إذا خطأ بملف الطبقات.
  - الثوابت: `CANVAS` و`UNSAFE` و`FONTS` و`MAX_WORDS = 4` و`YOUTUBE_MAX_BYTES`.
  - `render._layout_text(layer, W, H) -> (font, px, placed)`: كل عنصر بـ `placed` هو `(x, y, text, kwargs, fill, line_index)`.
  - `render._fill_image(fill, y0, y1, size) -> Image`: لون أو تدرج.
  - `render._text_layer(canvas, layer) -> (x0, y0, x1, y1)`: حدود الحبر الحقيقي.

- [ ] **الخطوة 1: اكتب الاختبار الفاشل** `designer/tests/test_render.py`
```python
import contextlib
import io
import json
import os
import sys
import tempfile
import unittest
from pathlib import Path

from PIL import Image, ImageDraw

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
import render  # noqa: E402
from render import SpecError  # noqa: E402

GRADIENT = {"gradient": ["#101018", "#303040"]}


class RenderTest(unittest.TestCase):
    @classmethod
    def setUpClass(cls):
        cls.tmp = tempfile.TemporaryDirectory()
        cls.dir = Path(cls.tmp.name)
        Image.new("RGB", (800, 800), (200, 30, 30)).save(cls.dir / "square.png")
        face = Image.new("RGBA", (300, 400))
        ImageDraw.Draw(face).ellipse((50, 20, 250, 380), fill=(220, 170, 110, 255))
        face.save(cls.dir / "face.png")
        Image.new("RGB", (300, 400), (220, 170, 110)).save(cls.dir / "face_opaque.jpg")
        Image.frombytes("RGB", (1280, 720), os.urandom(1280 * 720 * 3)).save(cls.dir / "noise.png")

    @classmethod
    def tearDownClass(cls):
        cls.tmp.cleanup()

    def render(self, spec):
        return render.render(spec, self.dir)

    def text(self, text, **kw):
        layer = {"type": "text", "text": text, "x": 0.5, "y": 0.4, "size": 0.12}
        layer.update(kw)
        return layer

    # المقاسات
    def test_youtube_size(self):
        img, _ = self.render({"size": "youtube", "background": GRADIENT})
        self.assertEqual(img.size, (1280, 720))

    def test_reels_size(self):
        img, _ = self.render({"size": "reels", "background": GRADIENT})
        self.assertEqual(img.size, (1080, 1920))

    def test_custom_size(self):
        img, _ = self.render({"size": [1080, 1350], "background": {"color": "#000000"}})
        self.assertEqual(img.size, (1080, 1350))

    def test_square_background_fills_without_stretching(self):
        img, _ = self.render({"size": "youtube", "background": {"image": "square.png"}})
        self.assertEqual(img.size, (1280, 720))
        self.assertEqual(img.getpixel((5, 5))[:3], (200, 30, 30))

    # أخطاء واضحة
    def test_unknown_size(self):
        with self.assertRaisesRegex(SpecError, "مقاس مو معروف"):
            self.render({"size": "tiktok", "background": GRADIENT})

    def test_missing_background(self):
        with self.assertRaisesRegex(SpecError, "ناقصه background"):
            self.render({"size": "youtube"})

    def test_missing_image_names_the_file(self):
        with self.assertRaisesRegex(SpecError, "ghost.png"):
            self.render({"background": GRADIENT, "layers": [{"type": "image", "src": "ghost.png"}]})

    def test_unknown_font(self):
        with self.assertRaisesRegex(SpecError, "خط مو معروف"):
            self.render({"background": GRADIENT, "layers": [self.text("هلا", font="Arial")]})

    def test_unknown_layer_type(self):
        with self.assertRaisesRegex(SpecError, "نوع مو معروف"):
            self.render({"background": GRADIENT, "layers": [{"type": "video"}]})

    def test_empty_text(self):
        with self.assertRaisesRegex(SpecError, "فارغة"):
            self.render({"background": GRADIENT, "layers": [self.text("  ")]})

    def test_bad_color(self):
        with self.assertRaisesRegex(SpecError, "لون غلط"):
            self.render({"background": {"color": "red"}})

    # التحذيرات
    def test_centered_text_no_warnings(self):
        _, warnings = self.render({"background": GRADIENT, "layers": [self.text("فلوس الناس")]})
        self.assertEqual(warnings, [])

    def test_text_over_youtube_timestamp_warns(self):
        spec = {"background": GRADIENT, "layers": [self.text("وين؟", x=0.92, y=0.92, size=0.1)]}
        _, warnings = self.render(spec)
        self.assertTrue(any("زاوية مدة الفيديو" in w for w in warnings), warnings)

    def test_reels_text_in_caption_zone_warns(self):
        spec = {"size": "reels", "background": GRADIENT, "layers": [self.text("وين؟", x=0.4, y=0.9, size=0.05)]}
        _, warnings = self.render(spec)
        self.assertTrue(any("منطقة الكابشن" in w for w in warnings), warnings)

    def test_too_many_words_warns(self):
        spec = {"background": GRADIENT, "layers": [self.text("هذا نص طويل\nهواية بالغلاف", size=0.08)]}
        _, warnings = self.render(spec)
        self.assertTrue(any("5 كلمات" in w for w in warnings), warnings)

    def test_text_overflowing_canvas_warns(self):
        spec = {"background": GRADIENT, "layers": [self.text("الإمبراطورية انهارت بالكامل", size=0.3)]}
        _, warnings = self.render(spec)
        self.assertTrue(any("برا حدود الصورة" in w for w in warnings), warnings)

    def test_opaque_face_with_outline_warns(self):
        layer = {"type": "image", "src": "face_opaque.jpg", "x": 0.7, "h": 0.8, "outline": {"color": "#FFFFFF"}}
        _, warnings = self.render({"background": GRADIENT, "layers": [layer]})
        self.assertTrue(any("ما بيها شفافية" in w for w in warnings), warnings)

    def test_transparent_face_with_outline_no_warning(self):
        layer = {"type": "image", "src": "face.png", "x": 0.7, "h": 0.8, "outline": {"color": "#FFFFFF"}}
        img, warnings = self.render({"background": GRADIENT, "layers": [layer]})
        self.assertEqual(warnings, [])
        self.assertEqual(img.getpixel((int(0.7 * 1280), 720 - 288))[:3], (220, 170, 110))  # نص الوجه

    # الكلمة المميزة
    def test_highlight_ignores_punctuation(self):
        layer = self.text("وين راحت؟", highlight={"word": "راحت", "color": "#FFD400"})
        _, _, placed = render._layout_text(layer, 1280, 720)
        fills = [p[4] for p in placed]
        self.assertEqual(sorted(fills), ["#FFD400", "#FFFFFF"])

    # ستايل أغلفة صاحب القناة: تدرج، ميلان، ورق ممزق
    def test_line_colors_white_then_gradient(self):
        layer = self.text("نهاية\nالكون!", size=0.2, line_colors=["#FFFFFF", ["#FFD000", "#FF6A00"]])
        img, warnings = self.render({"background": {"color": "#000000"}, "layers": [layer]})
        self.assertEqual(warnings, [])
        _, _, placed = render._layout_text(layer, 1280, 720)
        self.assertEqual([p[4] for p in placed], ["#FFFFFF", ["#FFD000", "#FF6A00"]])

    def test_gradient_fill_top_differs_from_bottom(self):
        fill = render._fill_image(["#FFD000", "#FF6A00"], 100, 300, (1280, 720))
        self.assertEqual(fill.getpixel((5, 50))[:3], (255, 208, 0))
        self.assertEqual(fill.getpixel((5, 400))[:3], (255, 106, 0))
        mid = fill.getpixel((5, 200))[1]
        self.assertTrue(106 < mid < 208, mid)

    def test_bad_gradient_fill(self):
        with self.assertRaisesRegex(SpecError, "لون الكتابة"):
            self.render({"background": GRADIENT, "layers": [self.text("هلا", color=["#FFFFFF"])]})

    def test_rotated_text_still_warns_in_unsafe_zone(self):
        spec = {"background": GRADIENT, "layers": [self.text("وين؟", x=0.9, y=0.9, size=0.1, rotate=6)]}
        _, warnings = self.render(spec)
        self.assertTrue(any("زاوية مدة الفيديو" in w for w in warnings), warnings)

    def test_brand_headline_template_has_no_false_warning(self):
        # قالب الكتابة الأساسي بـ studio/brand.md: الحبر بعيد عن الزاوية، فلازم ماكو تحذير
        layer = {"type": "text", "text": "نهاية\nالكون!", "font": "Baloo", "x": 0.27, "y": 0.47, "size": 0.2,
                 "line_spacing": 0.62, "line_colors": ["#FFFFFF", ["#FFC400", "#FF6A00"]],
                 "stroke": {"color": "#0B0B0F", "width": 0.1}, "shadow": True, "rotate": 5}
        _, warnings = self.render({"background": GRADIENT, "layers": [layer]})
        self.assertEqual(warnings, [])

    def test_text_bbox_is_real_ink(self):
        canvas = Image.new("RGBA", (1280, 720), (0, 0, 0, 255))
        x0, y0, x1, y1 = render._text_layer(canvas, self.text("وين", x=0.5, y=0.5, size=0.1))
        self.assertTrue(560 < x0 < 640 < x1 < 720, (x0, x1))
        self.assertTrue(290 < y0 < 360 < y1 < 430, (y0, y1))

    def test_paper_box_is_light(self):
        layer = self.text("مش لوحدنا", x=0.5, y=0.5, size=0.12, color="#111111",
                          box={"style": "paper", "pad": 0.4})
        img, _ = self.render({"background": {"color": "#000000"}, "layers": [layer]})
        bright = sum(1 for v in img.convert("L").getdata() if v > 150)
        self.assertGreater(bright, 40000)  # الورق الفاتح واضح خلف الكتابة الداكنة

    def test_background_finish_options(self):
        spec = {"background": {"image": "square.png", "contrast": 1.2, "saturation": 1.3, "sharpen": 1}}
        img, _ = self.render(spec)
        self.assertEqual(img.size, (1280, 720))

    # الأشكال والحفظ والفحص
    def test_shapes_render(self):
        layers = [
            {"type": "shape", "shape": "arrow", "from": [0.2, 0.8], "to": [0.4, 0.5]},
            {"type": "shape", "shape": "circle", "center": [0.7, 0.5], "r": 0.2},
            {"type": "shape", "shape": "rect", "box": [0.1, 0.1, 0.3, 0.3], "fill": True, "color": "#FFD400"},
        ]
        img, _ = self.render({"background": GRADIENT, "layers": layers})
        self.assertEqual(img.getpixel((int(0.2 * 1280), int(0.2 * 720)))[:3], (255, 212, 0))

    def test_unknown_shape(self):
        with self.assertRaisesRegex(SpecError, "شكل مو معروف"):
            self.render({"background": GRADIENT, "layers": [{"type": "shape", "shape": "star"}]})

    def test_guides_paint_unsafe_zone_red(self):
        img, _ = self.render({"background": {"color": "#000000"}})
        guided = render.draw_guides(img, "youtube")
        r, g, b, _ = guided.getpixel((1270, 710))
        self.assertGreater(r, 60)
        self.assertLess(g, 20)

    def run_cli(self, spec: dict, out_name: str) -> tuple[int, str]:
        spec_path = self.dir / f"{out_name}.json"
        spec_path.write_text(json.dumps(spec, ensure_ascii=False), encoding="utf-8")
        buf = io.StringIO()
        with contextlib.redirect_stdout(buf):
            code = render.main([str(spec_path), "-o", str(self.dir / out_name)])
        return code, buf.getvalue()

    def test_cli_saves_jpg_under_2mb(self):
        code, out = self.run_cli({"background": {"image": "noise.png"}}, "ok.jpg")
        self.assertEqual(code, 0)
        self.assertIn("انحفظ", out)
        self.assertLess((self.dir / "ok.jpg").stat().st_size, render.YOUTUBE_MAX_BYTES)

    def test_cli_warns_when_png_too_big_for_youtube(self):
        code, out = self.run_cli({"background": {"image": "noise.png"}}, "big.png")
        self.assertEqual(code, 0)
        self.assertIn("أكبر من 2MB", out)

    def test_cli_bad_spec_returns_error(self):
        code, out = self.run_cli({"size": "tiktok", "background": GRADIENT}, "bad.jpg")
        self.assertEqual(code, 1)
        self.assertIn("✗ خطأ", out)

    def test_cli_broken_json(self):
        bad = self.dir / "broken.json"
        bad.write_text("{ not json", encoding="utf-8")
        buf = io.StringIO()
        with contextlib.redirect_stdout(buf):
            code = render.main([str(bad), "-o", str(self.dir / "x.jpg")])
        self.assertEqual(code, 1)
        self.assertIn("غلط بالصيغة", buf.getvalue())


if __name__ == "__main__":
    unittest.main()
```

- [ ] **الخطوة 2: شغّله وتأكد إنه يفشل**

شغّل: `python3 -m unittest designer/tests/test_render.py -v`
المتوقع: فشل بـ `ModuleNotFoundError: No module named 'render'`

- [ ] **الخطوة 3: اكتب** `designer/render.py`
```python
"""المركّب: يقرا ملف طبقات JSON ويطلع غلاف PNG أو JPG.

    python3 designer/render.py spec.json -o out.jpg [--guides]

كل المقاسات بالملف نسبية (من 0 لـ 1) حتى نفس الفكرة تتحول لمقاس ثاني.
المسارات النسبية تنحسب من مجلد ملف الطبقات، وإذا ما لگاها، من جذر الريبو.
"""
import argparse
import json
import math
import random
import sys
from pathlib import Path

from PIL import Image, ImageDraw, ImageEnhance, ImageFilter, ImageOps

sys.path.insert(0, str(Path(__file__).resolve().parent))
from arabic import load_font, shape_word, visual_words  # noqa: E402

ROOT = Path(__file__).resolve().parent.parent
FONTS_DIR = Path(__file__).resolve().parent / "fonts"
FONTS = {
    "Baloo": "BalooBhaijaan2-ExtraBold.ttf",
    "Rubik": "Rubik-Black.ttf",
    "Cairo": "Cairo-Black.ttf",
    "Lalezar": "Lalezar-Regular.ttf",
}
CANVAS = {"youtube": (1280, 720), "reels": (1080, 1920)}
# المناطق الي تغطيها واجهة التطبيق: (اسم، x0، y0، x1، y1) كنسب من العرض والارتفاع
UNSAFE = {
    "youtube": [
        ("زاوية مدة الفيديو", 0.80, 0.82, 1.0, 1.0),
        ("زاوية مدة الفيديو", 0.0, 0.82, 0.20, 1.0),
    ],
    "reels": [
        ("أعلى الشاشة", 0.0, 0.0, 1.0, 0.125),
        ("منطقة الكابشن", 0.0, 0.75, 1.0, 1.0),
        ("أزرار اليمين", 0.85, 0.0, 1.0, 1.0),
    ],
}
MAX_WORDS = 4
YOUTUBE_MAX_BYTES = 2 * 1024 * 1024
PUNCT = "؟?!.,،:؛\"'«»()"


class SpecError(ValueError):
    """خطأ بملف الطبقات، رسالته بالعربي."""


def load_spec(path) -> dict:
    try:
        return json.loads(Path(path).read_text(encoding="utf-8"))
    except FileNotFoundError:
        raise SpecError(f"ملف الطبقات مو موجود: {path}")
    except json.JSONDecodeError as e:
        raise SpecError(f"ملف الطبقات بيه غلط بالصيغة (سطر {e.lineno}): {path}")


def size_name(spec: dict):
    """اسم المقاس (youtube أو reels)، أو None إذا المقاس أرقام."""
    size = spec.get("size", "youtube")
    return size if isinstance(size, str) else None


def canvas_size(spec: dict) -> tuple[int, int]:
    size = spec.get("size", "youtube")
    if isinstance(size, str):
        if size not in CANVAS:
            raise SpecError(f"مقاس مو معروف: {size}. المسموح: {', '.join(CANVAS)}")
        return CANVAS[size]
    if isinstance(size, list) and len(size) == 2 and all(isinstance(v, int) and v > 0 for v in size):
        return size[0], size[1]
    raise SpecError("size لازم يكون youtube أو reels أو [عرض، ارتفاع]")


def resolve(src: str, base_dir: Path) -> Path:
    p = Path(src)
    for candidate in (p, base_dir / p, ROOT / p):
        if candidate.is_file():
            return candidate
    raise SpecError(f"الصورة مو موجودة: {src}")


def hex_rgba(color: str, alpha: int = 255) -> tuple[int, int, int, int]:
    c = str(color).lstrip("#")
    try:
        return int(c[0:2], 16), int(c[2:4], 16), int(c[4:6], 16), alpha
    except ValueError:
        raise SpecError(f"لون غلط: {color}. اكتبه مثل #FFD400")


def _background(bg: dict, size, base_dir: Path) -> Image.Image:
    w, h = size
    if "image" in bg:
        img = Image.open(resolve(bg["image"], base_dir)).convert("RGB")
        focus = tuple(bg.get("focus", [0.5, 0.5]))
        img = ImageOps.fit(img, size, Image.LANCZOS, centering=focus)
    elif "gradient" in bg:
        top, bottom = bg["gradient"]
        mask = Image.linear_gradient("L").resize(size)
        img = Image.composite(Image.new("RGB", size, hex_rgba(bottom)[:3]),
                              Image.new("RGB", size, hex_rgba(top)[:3]), mask)
    elif "color" in bg:
        img = Image.new("RGB", size, hex_rgba(bg["color"])[:3])
    else:
        raise SpecError("background لازم بيه image أو gradient أو color")
    if bg.get("blur"):
        img = img.filter(ImageFilter.GaussianBlur(bg["blur"] * h))
    if bg.get("darken"):
        img = ImageEnhance.Brightness(img).enhance(1 - bg["darken"])
    if bg.get("contrast"):
        img = ImageEnhance.Contrast(img).enhance(bg["contrast"])
    if bg.get("saturation"):
        img = ImageEnhance.Color(img).enhance(bg["saturation"])
    if bg.get("sharpen"):
        img = img.filter(ImageFilter.UnsharpMask(radius=2, percent=int(120 * bg["sharpen"]), threshold=3))
    img = img.convert("RGBA")
    if bg.get("vignette"):
        mask = Image.radial_gradient("L").resize(size)
        mask = mask.point(lambda v: int(v * bg["vignette"]))
        img = Image.composite(Image.new("RGBA", size, (0, 0, 0, 255)), img, mask)
    return img


def _silhouette(alpha: Image.Image, grow: float, color: str) -> Image.Image:
    """حد ملون حول الشخص: نكبّر شكله بمقدار grow بكسل ونلونه."""
    spread = alpha.filter(ImageFilter.GaussianBlur(grow)).point(lambda a: 255 if a > 8 else 0)
    layer = Image.new("RGBA", alpha.size, hex_rgba(color))
    layer.putalpha(spread)
    return layer


def _image_layer(canvas: Image.Image, layer: dict, base_dir: Path) -> list[str]:
    W, H = canvas.size
    warnings = []
    img = Image.open(resolve(layer["src"], base_dir)).convert("RGBA")
    if img.getchannel("A").getextrema()[0] == 255 and ("outline" in layer or "glow" in layer):
        warnings.append(f"الصورة {layer['src']} ما بيها شفافية، فالحد راح يطلع مربع. شيل خلفيتها أول.")
    if "h" in layer:
        th = max(1, int(layer["h"] * H))
        img = img.resize((max(1, int(img.width * th / img.height)), th), Image.LANCZOS)
    elif "w" in layer:
        tw = max(1, int(layer["w"] * W))
        img = img.resize((tw, max(1, int(img.height * tw / img.width))), Image.LANCZOS)
    if layer.get("flip"):
        img = ImageOps.mirror(img)
    pad = int(0.05 * H)
    padded = Image.new("RGBA", (img.width + 2 * pad, img.height + 2 * pad))
    padded.paste(img, (pad, pad), img)
    alpha = padded.getchannel("A")
    out = Image.new("RGBA", padded.size)
    if "glow" in layer:
        g = layer["glow"]
        glow = alpha.filter(ImageFilter.GaussianBlur(g.get("radius", 0.02) * H))
        glow_layer = Image.new("RGBA", padded.size, hex_rgba(g["color"]))
        glow_layer.putalpha(glow)
        out = Image.alpha_composite(out, glow_layer)
    if "outline" in layer:
        o = layer["outline"]
        out = Image.alpha_composite(out, _silhouette(alpha, o.get("width", 0.012) * H, o["color"]))
    out = Image.alpha_composite(out, padded)
    x, y = layer.get("x", 0.5) * W, layer.get("y", 1.0) * H
    anchor = layer.get("anchor", "bottom-center")
    left = int(x - out.width / 2)
    if anchor == "bottom-center":
        top = int(y - img.height - pad)
    elif anchor == "center":
        top = int(y - out.height / 2)
    else:
        raise SpecError(f"anchor للصورة مو معروف: {anchor}. المسموح: bottom-center، center")
    full = Image.new("RGBA", canvas.size)
    full.paste(out, (left, top), out)
    canvas.alpha_composite(full)
    return warnings


def _layout_text(layer: dict, W: int, H: int):
    """يرجع الخط وحجمه ومكان كل كلمة: [(x، y، النص، معاملات، اللون، رقم السطر)].

    اللون نص مثل "#FFFFFF"، أو تدرج عمودي [لون فوق، لون جوه].
    """
    font_name = layer.get("font", "Baloo")
    if font_name not in FONTS:
        raise SpecError(f"خط مو معروف: {font_name}. المتوفر: {', '.join(FONTS)}")
    px = max(8, int(layer.get("size", 0.14) * H))
    font = load_font(FONTS_DIR / FONTS[font_name], px)
    ascent, descent = font.getmetrics()
    line_h = int((ascent + descent) * layer.get("line_spacing", 0.95))
    space = font.getlength(" ")
    color = layer.get("color", "#FFFFFF")
    line_colors = layer.get("line_colors", [])
    hl = layer.get("highlight", {})
    hl_words = {w.strip(PUNCT) for w in hl.get("words", [])}
    if "word" in hl:
        hl_words.add(hl["word"].strip(PUNCT))
    lines = []
    for line in layer["text"].split("\n"):
        words = []
        for word in visual_words(line):
            text, kw = shape_word(word)
            words.append((word, text, kw, font.getlength(text, **kw)))
        width = sum(w[3] for w in words) + space * max(0, len(words) - 1)
        lines.append((words, width))
    x, y = layer.get("x", 0.5) * W, layer.get("y", 0.5) * H
    align = layer.get("align", "center")
    top = y - line_h * len(lines) / 2
    placed = []
    for i, (words, width) in enumerate(lines):
        if align == "center":
            lx = x - width / 2
        elif align == "right":
            lx = x - width
        elif align == "left":
            lx = x
        else:
            raise SpecError(f"align مو معروف: {align}. المسموح: center، right، left")
        ly = top + i * line_h
        base = line_colors[i] if i < len(line_colors) else color
        for word, text, kw, wlen in words:
            fill = hl.get("color", "#FFD400") if word.strip(PUNCT) in hl_words else base
            placed.append((lx, ly, text, kw, fill, i))
            lx += wlen + space
    return font, px, placed


def _fill_image(fill, y0: float, y1: float, size) -> Image.Image:
    """صورة بحجم الغلاف: لون واحد، أو تدرج عمودي من y0 لـ y1."""
    W, H = size
    if isinstance(fill, str):
        return Image.new("RGBA", size, hex_rgba(fill))
    if not (isinstance(fill, list) and len(fill) == 2):
        raise SpecError(f"لون الكتابة لازم لون واحد أو [لون فوق، لون جوه]: {fill}")
    top, bottom = hex_rgba(fill[0]), hex_rgba(fill[1])
    y0 = max(0, min(H - 1, int(y0)))
    y1 = max(y0 + 1, min(H, int(y1)))
    col = Image.new("RGBA", (1, H), bottom)
    if y0 > 0:
        col.paste(top, (0, 0, 1, y0))
    mask = Image.linear_gradient("L").resize((1, y1 - y0))
    col.paste(Image.composite(Image.new("RGBA", (1, y1 - y0), bottom),
                              Image.new("RGBA", (1, y1 - y0), top), mask), (0, y0))
    return col.resize(size, Image.NEAREST)


def _paper(target: Image.Image, box, color: str, seed: str) -> None:
    """رقعة ورق ممزق الأطراف خلف الكتابة، مع ظل خفيف."""
    rnd = random.Random(seed)
    x0, y0, x1, y1 = box
    jit = (y1 - y0) * 0.05
    step = max(8.0, (x1 - x0) / 20)
    pts = []
    for ax, ay, bx, by in ((x0, y0, x1, y0), (x1, y0, x1, y1), (x1, y1, x0, y1), (x0, y1, x0, y0)):
        n = max(2, int(math.hypot(bx - ax, by - ay) / step))
        for i in range(n):
            t = i / n
            pts.append((ax + (bx - ax) * t + rnd.uniform(-jit, jit), ay + (by - ay) * t + rnd.uniform(-jit, jit)))
    mask = Image.new("L", target.size)
    ImageDraw.Draw(mask).polygon(pts, fill=255)
    shadow_mask = Image.new("L", target.size)
    shadow_mask.paste(mask, (int(jit), int(jit)))
    shadow = Image.new("RGBA", target.size, (0, 0, 0, 0))
    shadow.putalpha(shadow_mask.point(lambda a: a * 150 // 255).filter(ImageFilter.GaussianBlur(max(1.0, jit))))
    target.alpha_composite(shadow)
    paper = Image.blend(Image.new("RGB", target.size, hex_rgba(color)[:3]),
                        Image.effect_noise(target.size, 60).convert("RGB"), 0.10).convert("RGBA")
    paper.putalpha(mask)
    target.alpha_composite(paper)


def _text_layer(canvas: Image.Image, layer: dict) -> tuple[int, int, int, int]:
    """يرسم الكتابة (مع الظل والصندوق والميلان) ويرجع حدود الحبر الحقيقي بالبكسل (x0، y0، x1، y1).

    الرسم يصير على لوحة أوسع من الغلاف بهامش M، حتى الحدود تنحسب صح
    حتى لو الكتابة طالعة برا الصورة.
    """
    W, H = canvas.size
    M = int(0.25 * max(W, H))
    size = (W + 2 * M, H + 2 * M)
    font, px, placed = _layout_text(layer, W, H)
    placed = [(x + M, y + M, t, kw, fill, line) for x, y, t, kw, fill, line in placed]
    stroke = layer.get("stroke")
    sw = int(stroke.get("width", 0.08) * px) if stroke else 0
    probe = ImageDraw.Draw(Image.new("RGBA", (1, 1)))
    boxes = [probe.textbbox((p[0], p[1]), p[2], font=font, anchor="la", stroke_width=sw, **p[3]) for p in placed]
    bbox = (min(b[0] for b in boxes), min(b[1] for b in boxes),
            max(b[2] for b in boxes), max(b[3] for b in boxes))
    line_span = {}
    for b, p in zip(boxes, placed):
        y0, y1 = line_span.get(p[5], (b[1], b[3]))
        line_span[p[5]] = (min(y0, b[1]), max(y1, b[3]))
    out = Image.new("RGBA", size)
    if layer.get("shadow"):
        shadow = Image.new("RGBA", size)
        d = ImageDraw.Draw(shadow)
        off = 0.05 * px
        for x, y, t, kw, *_ in placed:
            d.text((x + off, y + off), t, font=font, anchor="la", fill=(0, 0, 0, 200),
                   stroke_width=sw, stroke_fill=(0, 0, 0, 200), **kw)
        out.alpha_composite(shadow.filter(ImageFilter.GaussianBlur(0.06 * px)))
    if "box" in layer:
        b = layer["box"]
        pad = int(b.get("pad", 0.18) * px)
        box = (bbox[0] - pad, bbox[1] - pad, bbox[2] + pad, bbox[3] + pad)
        if b.get("style") == "paper":
            _paper(out, box, b.get("color", "#E9E1D2"), layer["text"])
        else:
            ImageDraw.Draw(out).rounded_rectangle(
                box, radius=int(b.get("radius", 0.1) * px), fill=hex_rgba(b.get("color", "#E50914")))
    if stroke:
        d = ImageDraw.Draw(out)
        sc = hex_rgba(stroke["color"])
        for x, y, t, kw, *_ in placed:
            d.text((x, y), t, font=font, anchor="la", fill=sc, stroke_width=sw, stroke_fill=sc, **kw)
    for x, y, t, kw, fill, line in placed:
        mask = Image.new("L", size)
        ImageDraw.Draw(mask).text((x, y), t, font=font, anchor="la", fill=255, **kw)
        out.paste(_fill_image(fill, *line_span[line], size), (0, 0), mask)
    angle = layer.get("rotate", 0)
    if angle:
        center = ((bbox[0] + bbox[2]) / 2, (bbox[1] + bbox[3]) / 2)
        out = out.rotate(angle, resample=Image.BICUBIC, center=center)
    ink = out.getchannel("A").point(lambda a: 255 if a > 40 else 0).getbbox()
    canvas.alpha_composite(out.crop((M, M, M + W, M + H)))
    if ink is None:
        return (0, 0, 0, 0)
    return (ink[0] - M, ink[1] - M, ink[2] - M, ink[3] - M)


def _shape_layer(canvas: Image.Image, layer: dict) -> None:
    W, H = canvas.size
    d = ImageDraw.Draw(canvas)
    color = hex_rgba(layer.get("color", "#E50914"))
    width = max(1, int(layer.get("width", 0.02) * H))
    kind = layer.get("shape")
    if kind == "arrow":
        (x0, y0), (x1, y1) = [(p[0] * W, p[1] * H) for p in (layer["from"], layer["to"])]
        ang = math.atan2(y1 - y0, x1 - x0)
        head = width * 3.2
        tip = (x1, y1)
        left = (x1 - head * math.cos(ang - 0.5), y1 - head * math.sin(ang - 0.5))
        right = (x1 - head * math.cos(ang + 0.5), y1 - head * math.sin(ang + 0.5))
        base = (x1 - head * 0.8 * math.cos(ang), y1 - head * 0.8 * math.sin(ang))
        border = (0, 0, 0, 255)
        d.line([(x0, y0), base], fill=border, width=width + max(4, width // 2))
        d.polygon([tip, left, right], fill=border, outline=border, width=max(4, width // 3))
        d.line([(x0, y0), base], fill=color, width=width)
        d.polygon([tip, left, right], fill=color)
    elif kind == "circle":
        cx, cy = layer["center"][0] * W, layer["center"][1] * H
        r = layer.get("r", 0.15) * H
        d.ellipse((cx - r, cy - r, cx + r, cy + r), outline=color, width=width)
    elif kind == "rect":
        x0, y0, x1, y1 = layer["box"]
        box = (x0 * W, y0 * H, x1 * W, y1 * H)
        radius = int(layer.get("radius", 0) * H)
        if layer.get("fill"):
            d.rounded_rectangle(box, radius=radius, fill=color)
        else:
            d.rounded_rectangle(box, radius=radius, outline=color, width=width)
    else:
        raise SpecError(f"شكل مو معروف: {kind}. المسموح: arrow، circle، rect")


def _overlaps(a, b) -> bool:
    return a[0] < b[2] and b[0] < a[2] and a[1] < b[3] and b[1] < a[3]


def render(spec: dict, base_dir) -> tuple[Image.Image, list[str]]:
    """يرسم الغلاف ويرجع الصورة وقائمة تحذيرات بالعربي."""
    size = canvas_size(spec)
    if "background" not in spec:
        raise SpecError("ملف الطبقات ناقصه background")
    base_dir = Path(base_dir)
    canvas = _background(spec["background"], size, base_dir)
    W, H = size
    zones = UNSAFE.get(size_name(spec), [])
    warnings, words = [], 0
    for i, layer in enumerate(spec.get("layers", []), 1):
        kind = layer.get("type")
        if kind == "image":
            warnings += _image_layer(canvas, layer, base_dir)
        elif kind == "text":
            if not str(layer.get("text", "")).strip():
                raise SpecError(f"الطبقة {i}: الكتابة فارغة")
            bbox = _text_layer(canvas, layer)
            words += len(layer["text"].split())
            for name, x0, y0, x1, y1 in zones:
                if _overlaps(bbox, (x0 * W, y0 * H, x1 * W, y1 * H)):
                    warnings.append(f"الكتابة \"{layer['text']}\" داخلة بمنطقة مغطاة ({name}).")
            if bbox[0] < 0 or bbox[1] < 0 or bbox[2] > W or bbox[3] > H:
                warnings.append(f"الكتابة \"{layer['text']}\" طالعة برا حدود الصورة.")
        elif kind == "shape":
            _shape_layer(canvas, layer)
        else:
            raise SpecError(f"الطبقة {i}: نوع مو معروف: {kind}. المسموح: image، text، shape")
    if words > MAX_WORDS:
        warnings.append(f"الكتابة {words} كلمات، والحد {MAX_WORDS}.")
    return canvas, warnings


def draw_guides(img: Image.Image, name) -> Image.Image:
    """يلوّن المناطق المغطاة بالأحمر، للفحص الداخلي بس. الإطار الأزرق = قص شبكة إنستگرام."""
    out = img.copy()
    overlay = Image.new("RGBA", img.size)
    d = ImageDraw.Draw(overlay)
    W, H = img.size
    for _, x0, y0, x1, y1 in UNSAFE.get(name, []):
        d.rectangle((x0 * W, y0 * H, x1 * W, y1 * H), fill=(255, 0, 0, 90), outline=(255, 0, 0, 255), width=3)
    if name == "reels":
        d.rectangle((0, 0.125 * H, W, 0.875 * H), outline=(0, 200, 255, 255), width=4)
    out.alpha_composite(overlay)
    return out


def save(img: Image.Image, out: Path) -> None:
    out = Path(out)
    out.parent.mkdir(parents=True, exist_ok=True)
    if out.suffix.lower() in (".jpg", ".jpeg"):
        img.convert("RGB").save(out, quality=92, optimize=True)
    else:
        img.save(out)


def main(argv=None) -> int:
    ap = argparse.ArgumentParser(description="يركّب غلاف من ملف طبقات JSON")
    ap.add_argument("spec")
    ap.add_argument("-o", "--out", required=True)
    ap.add_argument("--guides", action="store_true", help="يرسم مناطق الأمان للفحص")
    args = ap.parse_args(argv)
    try:
        spec = load_spec(args.spec)
        img, warnings = render(spec, Path(args.spec).parent)
    except SpecError as e:
        print(f"✗ خطأ: {e}")
        return 1
    if args.guides:
        img = draw_guides(img, size_name(spec))
    out = Path(args.out)
    save(img, out)
    if size_name(spec) == "youtube" and out.stat().st_size > YOUTUBE_MAX_BYTES:
        warnings.append("حجم الملف أكبر من 2MB ويوتيوب ما يقبله. احفظه .jpg")
    for w in warnings:
        print(f"⚠️ {w}")
    print(f"✓ انحفظ: {out}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **الخطوة 4: شغّل الاختبارات**

شغّل: `python3 -m unittest discover -s designer/tests -v`
المتوقع: 43 اختبار `OK` (9 من المهمة 1 و34 جديدة)

- [ ] **الخطوة 5: مثال وفحص بالعين**

`designer/examples/sample-youtube.json`:
```json
{
  "size": "youtube",
  "background": {"gradient": ["#06121A", "#0F3A44"], "vignette": 0.5},
  "layers": [
    {"type": "text", "text": "نهاية\nالكون!", "font": "Baloo", "x": 0.27, "y": 0.47, "size": 0.2,
     "line_spacing": 0.62, "line_colors": ["#FFFFFF", ["#FFC400", "#FF6A00"]],
     "stroke": {"color": "#0B0B0F", "width": 0.1}, "shadow": true, "rotate": 5}
  ]
}
```
شغّل:
```bash
python3 designer/render.py designer/examples/sample-youtube.json -o /tmp/sample.jpg
```
المتوقع: `✓ انحفظ` **بدون** أي سطر ⚠️.

بعدها افتح `/tmp/sample.jpg` بأداة Read، وتأكد:
- "نهاية" فوق بالأبيض، و"الكون!" جوه بتدرج أصفر وبرتقالي.
- الحروف متصلة، وحد أسود عريض، والكتابة مايلة شوية (الجهة اليمنى لفوق).

- [ ] **الخطوة 6: Commit**
```bash
git add designer/render.py designer/tests/test_render.py designer/examples/sample-youtube.json
git commit -m "Add layer-based thumbnail renderer with safe-zone warnings"
```

---

### المهمة 3: ورقة المقارنة (`sheet.py`)

**الملفات:**
- إنشاء: `designer/sheet.py` و`designer/tests/test_sheet.py`

**الواجهات:**
- تستخدم: `arabic.load_font`.
- تنتج:
  - `sheet.make_sheet(paths: list, labels: list | None = None) -> Image.Image`
  - `sheet.main(argv) -> int`
  - الثوابت: `SHEET_SIZE = (1340, 1028)` و`TILE` و`MINI = (168, 94)` و`GAP` و`LIGHT_ROW_Y` و`DARK_ROW_Y` و`SHEET_W`.

- [ ] **الخطوة 1: اكتب الاختبار الفاشل** `designer/tests/test_sheet.py`
```python
import contextlib
import io
import sys
import tempfile
import unittest
from pathlib import Path

from PIL import Image

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
import sheet  # noqa: E402


class SheetTest(unittest.TestCase):
    @classmethod
    def setUpClass(cls):
        cls.tmp = tempfile.TemporaryDirectory()
        cls.dir = Path(cls.tmp.name)
        cls.paths = []
        for name, color in zip("ABCDE", [(255, 0, 0), (0, 255, 0), (0, 0, 255), (255, 255, 0), (0, 0, 0)]):
            p = cls.dir / f"{name}.png"
            Image.new("RGB", (1280, 720), color).save(p)
            cls.paths.append(p)

    @classmethod
    def tearDownClass(cls):
        cls.tmp.cleanup()

    def test_four_images_sheet_size(self):
        img = sheet.make_sheet(self.paths[:4])
        self.assertEqual(img.size, sheet.SHEET_SIZE)

    def test_tile_b_is_top_right(self):
        img = sheet.make_sheet(self.paths[:4])
        x = sheet.GAP * 2 + sheet.TILE[0] + sheet.TILE[0] // 2
        self.assertEqual(img.getpixel((x, sheet.GAP + 200)), (0, 255, 0))

    def test_mini_previews_on_light_and_dark_rows(self):
        img = sheet.make_sheet(self.paths[:1])
        cx = sheet.GAP + sheet.MINI[0] // 2
        self.assertEqual(img.getpixel((cx, sheet.LIGHT_ROW_Y + 40))[0], 255)
        self.assertEqual(img.getpixel((cx, sheet.DARK_ROW_Y + 40))[0], 255)
        self.assertEqual(img.getpixel((sheet.SHEET_W - 5, sheet.LIGHT_ROW_Y + 40)), (255, 255, 255))
        self.assertEqual(img.getpixel((sheet.SHEET_W - 5, sheet.DARK_ROW_Y + 40)), (15, 15, 15))

    def test_one_image_ok(self):
        self.assertEqual(sheet.make_sheet(self.paths[:1]).size, sheet.SHEET_SIZE)

    def test_five_images_rejected(self):
        with self.assertRaisesRegex(ValueError, "من 1 لـ 4"):
            sheet.make_sheet(self.paths)

    def test_cli_writes_file(self):
        out = self.dir / "grid.jpg"
        buf = io.StringIO()
        with contextlib.redirect_stdout(buf):
            code = sheet.main([str(p) for p in self.paths[:4]] + ["-o", str(out)])
        self.assertEqual(code, 0)
        self.assertTrue(out.is_file())

    def test_cli_missing_file(self):
        buf = io.StringIO()
        with contextlib.redirect_stdout(buf):
            code = sheet.main([str(self.dir / "nope.jpg"), "-o", str(self.dir / "x.jpg")])
        self.assertEqual(code, 1)
        self.assertIn("✗ خطأ", buf.getvalue())


if __name__ == "__main__":
    unittest.main()
```

- [ ] **الخطوة 2: شغّله وتأكد إنه يفشل**

شغّل: `python3 -m unittest designer/tests/test_sheet.py -v`
المتوقع: فشل بـ `ModuleNotFoundError: No module named 'sheet'`

- [ ] **الخطوة 3: اكتب** `designer/sheet.py`
```python
"""ورقة المقارنة: لحد 4 أغلفة بصورة وحدة، وتحتها نفس الأغلفة بحجم الموبايل الصغير.

    python3 designer/sheet.py A.jpg B.jpg C.jpg D.jpg -o grid.jpg

الصف الصغير هو "اختبار الثانية الوحدة": إذا ما تنقرا هنا، ما تنقرا بالموبايل.
فوق خلفية بيضاء وتحت خلفية داكنة، مثل وضعي يوتيوب.
"""
import argparse
import sys
from pathlib import Path

from PIL import Image, ImageDraw

sys.path.insert(0, str(Path(__file__).resolve().parent))
from arabic import load_font  # noqa: E402

FONT = Path(__file__).resolve().parent / "fonts" / "Cairo-Black.ttf"
TILE = (640, 360)
MINI = (168, 94)
GAP = 20
SHEET_W = 2 * TILE[0] + 3 * GAP
GRID_H = 2 * TILE[1] + 3 * GAP
STRIP_H = 2 * MINI[1] + 3 * GAP
SHEET_SIZE = (SHEET_W, GRID_H + STRIP_H)
LIGHT_ROW_Y = GRID_H + GAP // 2
DARK_ROW_Y = GRID_H + GAP + MINI[1] + GAP


def make_sheet(paths: list, labels: list | None = None) -> Image.Image:
    if not 1 <= len(paths) <= 4:
        raise ValueError("ورقة المقارنة تاخذ من 1 لـ 4 صور")
    labels = labels or [Path(p).stem for p in paths]
    sheet = Image.new("RGB", SHEET_SIZE, (40, 40, 40))
    d = ImageDraw.Draw(sheet)
    big = load_font(FONT, 34)
    small = load_font(FONT, 16)
    d.rectangle((0, GRID_H, SHEET_W, DARK_ROW_Y - GAP // 2), fill=(255, 255, 255))
    d.rectangle((0, DARK_ROW_Y - GAP // 2, SHEET_W, SHEET_SIZE[1]), fill=(15, 15, 15))
    for i, (path, label) in enumerate(zip(paths, labels)):
        img = Image.open(path).convert("RGB")
        x = GAP + (i % 2) * (TILE[0] + GAP)
        y = GAP + (i // 2) * (TILE[1] + GAP)
        sheet.paste(img.resize(TILE, Image.LANCZOS), (x, y))
        d.ellipse((x + 12, y + 12, x + 68, y + 68), fill=(255, 212, 0), outline=(0, 0, 0), width=3)
        d.text((x + 40, y + 40), label, font=big, fill=(0, 0, 0), anchor="mm")
        mini = img.resize(MINI, Image.LANCZOS)
        mx = GAP + i * (MINI[0] + GAP * 2)
        sheet.paste(mini, (mx, LIGHT_ROW_Y))
        sheet.paste(mini, (mx, DARK_ROW_Y))
        d.text((mx + MINI[0] + 4, LIGHT_ROW_Y), label, font=small, fill=(0, 0, 0))
    return sheet


def main(argv=None) -> int:
    ap = argparse.ArgumentParser(description="يجمع الأغلفة بورقة مقارنة")
    ap.add_argument("images", nargs="+")
    ap.add_argument("-o", "--out", required=True)
    args = ap.parse_args(argv)
    try:
        sheet = make_sheet([Path(p) for p in args.images])
    except (ValueError, FileNotFoundError) as e:
        print(f"✗ خطأ: {e}")
        return 1
    out = Path(args.out)
    out.parent.mkdir(parents=True, exist_ok=True)
    if out.suffix.lower() in (".jpg", ".jpeg"):
        sheet.save(out, quality=90)
    else:
        sheet.save(out)
    print(f"✓ انحفظ: {out}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **الخطوة 4: شغّل الاختبارات**

شغّل: `python3 -m unittest discover -s designer/tests -v`
المتوقع: 50 اختبار `OK`

- [ ] **الخطوة 5: Commit**
```bash
git add designer/sheet.py designer/tests/test_sheet.py
git commit -m "Add comparison sheet with mobile-size preview rows"
```

---

### المهمة 4: قص الوجه (`cutout.py`)

**الملفات:**
- إنشاء: `designer/cutout.py` و`designer/tests/test_cutout.py`

**الواجهات:**
- تنتج:
  - `cutout.trim(img, margin=0.02) -> Image.Image`
  - `cutout.cut(path) -> Image.Image`: يحتاج `rembg`.
  - `cutout.main(argv) -> int`
  - `cutout.MODEL = "u2net_human_seg"`

- [ ] **الخطوة 1: اكتب الاختبار الفاشل** `designer/tests/test_cutout.py`
```python
import sys
import unittest
from pathlib import Path

from PIL import Image, ImageDraw

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
import cutout  # noqa: E402


class TrimTest(unittest.TestCase):
    def test_trim_crops_to_person(self):
        img = Image.new("RGBA", (1000, 1000))
        ImageDraw.Draw(img).rectangle((300, 200, 599, 899), fill=(200, 150, 100, 255))
        out = cutout.trim(img, margin=0)
        self.assertEqual(out.size, (300, 700))

    def test_trim_keeps_small_margin(self):
        img = Image.new("RGBA", (1000, 1000))
        ImageDraw.Draw(img).rectangle((300, 200, 599, 899), fill=(200, 150, 100, 255))
        out = cutout.trim(img, margin=0.02)
        self.assertEqual(out.size, (340, 740))

    def test_trim_fully_transparent_fails_clearly(self):
        with self.assertRaisesRegex(ValueError, "شفافة بالكامل"):
            cutout.trim(Image.new("RGBA", (100, 100)))


if __name__ == "__main__":
    unittest.main()
```

- [ ] **الخطوة 2: شغّله وتأكد إنه يفشل**

شغّل: `python3 -m unittest designer/tests/test_cutout.py -v`
المتوقع: فشل بـ `ModuleNotFoundError: No module named 'cutout'`

- [ ] **الخطوة 3: اكتب** `designer/cutout.py`
```python
"""يشيل الخلفية عن صورة شخص ويقص الفراغ الشفاف حوله.

    python3 designer/cutout.py in.png -o designer/faces/cut/shock-1.png

أول مرة يحتاج أداة القص (حجمها كبير، فما تنثبت إلا وقت الحاجة):
    pip install -q "rembg[cpu]"
وأول تشغيل ينزّل نموذج القص (176MB تقريباً) لـ ~/.rembg.
"""
import argparse
import sys
from pathlib import Path

from PIL import Image

MODEL = "u2net_human_seg"


def trim(img: Image.Image, margin: float = 0.02) -> Image.Image:
    """يقص الفراغ الشفاف حول الشخص، ويخلي هامش صغير."""
    alpha = img.getchannel("A").point(lambda a: 255 if a > 10 else 0)
    bbox = alpha.getbbox()
    if bbox is None:
        raise ValueError("الصورة طلعت شفافة بالكامل، القص فشل")
    m = int(margin * max(img.size))
    x0, y0, x1, y1 = bbox
    return img.crop((max(0, x0 - m), max(0, y0 - m), min(img.width, x1 + m), min(img.height, y1 + m)))


def cut(path: Path) -> Image.Image:
    try:
        from rembg import new_session, remove
    except ImportError:
        raise RuntimeError('أداة القص مو مثبتة. شغّل: pip install -q "rembg[cpu]"')
    img = Image.open(path).convert("RGB")
    return trim(remove(img, session=new_session(MODEL)).convert("RGBA"))


def main(argv=None) -> int:
    ap = argparse.ArgumentParser(description="يشيل الخلفية عن صورة شخص")
    ap.add_argument("image")
    ap.add_argument("-o", "--out", required=True)
    args = ap.parse_args(argv)
    try:
        result = cut(Path(args.image))
    except (RuntimeError, ValueError, FileNotFoundError) as e:
        print(f"✗ خطأ: {e}")
        return 1
    out = Path(args.out)
    out.parent.mkdir(parents=True, exist_ok=True)
    result.save(out.with_suffix(".png"))
    print(f"✓ انحفظ: {out.with_suffix('.png')}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **الخطوة 4: شغّل الاختبارات**

شغّل: `python3 -m unittest discover -s designer/tests -v`
المتوقع: 53 اختبار `OK`

- [ ] **الخطوة 5: تأكد إن الرسالة واضحة بدون rembg**

شغّل: `python3 designer/cutout.py designer/examples/sample-youtube.json -o /tmp/x.png`
المتوقع إذا `rembg` مو مثبت: `✗ خطأ: أداة القص مو مثبتة…`، والرقم 1.
(القص الحقيقي ينجرب بالمهمة 9 على صورة حقيقية.)

- [ ] **الخطوة 6: Commit**
```bash
git add designer/cutout.py designer/tests/test_cutout.py
git commit -m "Add local background removal for face photos"
```

---

### المهمة 5: هوية القناة ومكتبة الإلهام

**الملفات:**
- إنشاء: `studio/brand.md` و`studio/style-references.md`

**الواجهات:**
- تستخدم: أسماء الخطوط (`Lalezar` و`Cairo`) ومفاتيح ملف الطبقات من المهمة 2.
- تنتج: ملفات يقراها سكيل المصمم (المهمة 6).

- [ ] **الخطوة 1: اكتب** `studio/brand.md`
````markdown
# هوية القناة البصرية (المصمم يقراها قبل كل تصميم)

> مبنية على "وصفة الستايل" بـ `studio/style-references.md`، من 5 أغلفة اختارها صاحب القناة (25 أيلول 2026).
> **الشكل:** غلاف سينمائي، المقدم داخل المشهد، واللغز باليسار، وكتابة سطرين مايلة.

## ألوان الكتابة (ثابتة بكل غلاف)
| الاسم | الكود | وين |
|---|---|---|
| أبيض | `#FFFFFF` | السطر الأول |
| تدرج النار | `["#FFC400", "#FF6A00"]` | السطر الثاني (من الأصفر فوق للبرتقالي جوه) |
| أسود فحمي | `#0B0B0F` | الحد العريض حول الكتابة |
| ورق قديم | `#E9E1D2` | رقعة الورق (البديل) |

## ألوان المشهد (تتغير حسب الموضوع)
لون مسيطر واحد، ولون متوهج يكمّله:

| جو الموضوع | المسيطر | المتوهج |
|---|---|---|
| غموض، بحر، أوبئة | أزرق بحري `teal` | أخضر سام |
| رعب، مخلوقات، ظلمة | كحلي | برتقالي فانوس |
| كوارث، فضاء، انفجار | أزرق الفضاء | أحمر وبرتقالي نار |
| نفس، جريمة، سيطرة | أحمر غامق | أبيض بارد |
| خداع، أسرار، ناس | أزرق ضبابي بارد | أحمر عيون |
| فلوس، شركات، انهيار | أسود وذهبي | أحمر خسارة |

## الخطوط (بـ `designer/fonts/`)
| الاسم بملف الطبقات | متى |
|---|---|
| `Baloo` | **الأساسي:** العنوان المدوّر العريض |
| `Rubik` | الكتابة على رقعة الورق |
| `Cairo` | أرقام أو كلام أطول (نادر) |
| `Lalezar` | احتياط |

## القوالب الجاهزة (انسخها بملف الطبقات)
**الكتابة الأساسية (سطرين، يسار، مايلة):**
```json
{"type": "text", "text": "نهاية\nالكون!", "font": "Baloo", "x": 0.27, "y": 0.47, "size": 0.2,
 "line_spacing": 0.62, "line_colors": ["#FFFFFF", ["#FFC400", "#FF6A00"]],
 "stroke": {"color": "#0B0B0F", "width": 0.1}, "shadow": true, "rotate": 5}
```

**رقعة الورق:**
```json
{"type": "text", "text": "إحنا مش\nلوحدنا!", "font": "Rubik", "x": 0.27, "y": 0.6, "size": 0.13,
 "line_spacing": 0.9, "color": "#141414", "box": {"style": "paper", "pad": 0.3}, "rotate": -4}
```

**المشهد الكامل (الأساسي):** Canva يولّد المشهد والمقدم داخله، والكود يضيف الكتابة واللمسة الأخيرة:
```json
{"image": "scene-A.png", "contrast": 1.12, "saturation": 1.2, "sharpen": 1, "vignette": 0.35}
```

**المقدم مقصوص (احتياط):** إذا Canva ما حافظ على شكل وجهه. ضو حافة بلون المشهد، **بدون حد أبيض**:
```json
{"type": "image", "src": "designer/faces/cut/<الاسم>.png", "x": 0.74, "y": 1.02, "h": 0.98,
 "glow": {"color": "#FF7A00", "radius": 0.025}}
```

## التكوينات
1. **المقدم يمين واللغز يسار** (الأساسي): المقدم يمين يباوع للكاميرا ويأشر أو يتفاعل، واللغز يتوهج يسار، والكتابة يسار فوق اللغز أو جنبه.
2. **متناظر بدون كتابة:** المقدم بالنص بنظرة جدية، وعلى جانبيه عنصرين رمزيين.
3. **المقدم وشخصية:** المقدم يمين، وشخصية مرتبطة بالقصة (تاريخية أو صاحب شركة) جنبه.
4. **الشباك:** الحدث الكبير يبين من شباك أو شاشة، والمقدم يتفاعل وياه.

## القواعد الثابتة
- المقدم **داخل المشهد** وبزي أو أداة من الموضوع. تعبيره قوي ويباوع للكاميرا.
- الكتابة سطرين، 2-3 كلمات (4 بالأكثر)، وتنتهي بـ "!" أو "؟"، وما تكرر العنوان.
- 3 عناصر بالأكثر: المقدم، واللغز، والكتابة.
- ماكو أسهم ولا دوائر ولا صناديق ملونة، ولا حد أبيض حول المقدم.
- **الكتابة العربية يكتبها الكود بس.** ممنوع نطلب من الذكاء الاصطناعي يكتب كلام داخل الصورة.

## مناطق الأمان (المركّب يحذّر إذا الكتابة دخلتها)
- **يوتيوب:** الزاويتين الجوّه فارغة من الكتابة، لأن مدة الفيديو تغطي وحدة منهن.
- **ريلز وتيك توك وشورتس:**
  - أعلى 12.5% وأوطى 25% وآخر 15% من اليمين فارغة من الكتابة.
  - الوجه والكتابة داخل الإطار الأزرق بملف الفحص (هذا قص شبكة إنستگرام).

## وصفة أوامر المشهد لـ Canva (بالإنگليزي، داخلياً)
```
Cinematic YouTube thumbnail scene without any text: <the man from the reference photo | a young man>
wearing <زي الموضوع>, <تعبير> expression looking at the camera, on the right third of the frame,
<يأشر أو ماسك شي> <اللغز> on the left glowing with <اللون المتوهج> light,
<المكان والجو>, dominant <اللون المسيطر> tones, strong rim light, high contrast, vivid saturated colors,
sharp details, photorealistic, empty dark space on the left third for a headline,
no text, no letters, no logos, no watermark
```
- **يوتيوب:** `aspectRatio: LANDSCAPE_16_9`.
- **الطولي:** `aspectRatio: PORTRAIT_9_16`، وبدّل `on the right third` بـ `in the lower middle`، و`empty ... left third` بـ `empty space in the upper third`.
- **صور وجهه:** ضيف رقم صورته (من `designer/faces.json`) بـ `imageReferences`، واكتب `the man from the reference photo` بالأمر.

## تفضيلات صاحب القناة
<!-- كل ملاحظة يگولها (عجبني/ما عجبني) تنكتب هنا بتاريخها -->
- **(25 أيلول 2026)** دز 5 أغلفة وگال: "هيج التصميم أحب يكون". الوصفة كاملة بـ `studio/style-references.md`.
````

- [ ] **الخطوة 2: اكتب** `studio/style-references.md`
```markdown
# مكتبة الإلهام (ذوق صاحب القناة)

المصمم يقرا هذا الملف قبل كل تصميم.

**نستلهم، ما ننسخ:** ناخذ التقنية (التكوين، والألوان، وطريقة الكتابة)، مو الغلاف نفسه. وجوه الناس الي بالمراجع ما تنستخدم أبداً.

## شلون تضيف مراجع (لصاحب القناة)
- **أسهل طريقة:** ارفق صور الأغلفة الي تعجبك مباشرة بالمحادثة، واكتب "هذني مراجع".
- **أو** دز روابط فيديوهات يوتيوب الي عاجبك غلافها.
- **أو** ارفعها لمجلد "مراجع الأغلفة" بـ Canva، وگول "حدّث المراجع".

## المراجع
أول 5 مراجع وصلت بـ 25 أيلول 2026، وصاحب القناة گال: "هيج التصميم أحب يكون".

| # | الموضوع | التكوين والوجه | الكتابة | الألوان والمؤثرات | ليش قوي | شنو ناخذ |
|---|---|---|---|---|---|---|
| 1 | "كورونا الجديد؟" | المقدم يمين (تقريباً 45% من العرض)، مصدوم ويباوع للكاميرا، لابس بدلة وقاية وواقي وجه، ويأشر على سفينة أشباح باليسار | سطرين باليسار: أبيض ثم تدرج برتقالي، حد أسود عريض، مايلة شوية، وتنتهي بـ "؟" | أزرق بحري داكن، وأخضر سام يتوهج من السفينة، وسماء عاصفة | المقدم **داخل القصة** بزي الموضوع، والإشارة تسحب العين للغز | الزي والإشارة، والتوهج الملون على العنصر |
| 2 | "إحنا مش لوحدنا!" | يمين، خايف ويباوع للكاميرا، ماسك فانوس ضوّه برتقالي، ومخلوقات تطلع من الظلمة باليسار | سطرين أسود على رقعة ورق ممزقة ومايلة | كحلي غامق، وضو برتقالي دافي يرسم حافة المقدم | التضاد بين الظلمة والضو، والخطر يبين من ورا | رقعة الورق، والضو الدافي بإيد المقدم |
| 3 | "نهاية الكون!" | يمين، إيديه على وجهه من الصدمة، لابس بدلة رائد فضاء، وأضواء إنذار حمرا. باليسار شباك يبين منه الأرض تنفجر | سطرين: أبيض ثم تدرج برتقالي، حد أسود، مايلة | أزرق الفضاء، وأحمر وبرتقالي النار | تعبير صدمة مبالغ بيه، والحدث الكارثي واضح من أول نظرة | لغة الجسد (الإيدين)، وشباك يأطّر الحدث |
| 4 | "شفرة اللاوعي!" | يمين ببدلة رسمية، ماسك خيوط دمية بيها دماغ، وجنبه شخصية تاريخية (فرويد) ماسكة كتاب | سطرين: أبيض (مع تطويل "شفـرة") ثم برتقالي، حد أسود | أحمر غامق مسيطر، وضباب أحمر، ولوحات دماغ بالخلفية | الرمز (الدمية = السيطرة) يشرح الفكرة بثانية | الرمز البصري، والشخصية التاريخية جنب المقدم |
| 5 | (بدون كتابة) | المقدم بالنص بنظرة جدية للكاميرا. يساره رجل كبير يبتسم بخبث، ويمينه إيد ماسكة قناع شيطان عيونه حمرا | ماكو كتابة | أزرق ضبابي بارد، وأحمر متوهج بعيون القناع، وظلال ناس عيونهم زرقة | التناظر، والقناع يلمّح للخداع بدون ولا كلمة | تكوين متناظر، وعنصر رمزي يغني عن الكتابة |

## وصفة الستايل
1. **المقدم بطل الغلاف:**
   - كبير، من الصدر وفوق، وياخذ 40-50% من العرض.
   - بالثلث اليمين غالباً، أو بالنص إذا التكوين متناظر.
   - يباوع للكاميرا بتعبير قوي: صدمة، خوف، شك، جدية.
2. **المقدم داخل المشهد، مو ملصوق:**
   - لابس زي الموضوع: بدلة وقاية، رائد فضاء، بدلة رسمية.
   - ماسك أداة (فانوس، خيوط دمية)، أو يأشر، أو يتفاعل.
   - **ماكو حد أبيض حوله.** بداله ضو حافة ملون من لون المشهد.
3. **اللغز باليسار:** الشي الي يدور عليه الموضوع (سفينة، مخلوقات، أرض تنفجر، شخصية تاريخية) يتوهج بلون مميز.
4. **الكتابة:**
   - سطرين، كل سطر كلمة أو كلمتين، باليسار، ومايلة 4-6 درجات.
   - السطر الأول أبيض، والثاني تدرج من الأصفر للبرتقالي.
   - حد أسود عريض وظل، وتنتهي بـ "!" أو "؟".
   - خط مدوّر وعريض جداً (`Baloo`).
   - **البديل:** رقعة ورق ممزق وكتابة سودة بخط `Rubik`.
5. **الألوان:**
   - لون واحد مسيطر على المشهد: أزرق بحري، أو كحلي، أو أحمر.
   - ولون متوهج يكمّله: أخضر سام، أو برتقالي نار.
   - تباين عالي، وتشبع قوي، وحدّة.
6. **سينمائي وواقعي:** صورة فوتوغرافية، وإضاءة درامية، وضباب ودخان.
7. **أحياناً بدون كتابة:** لما العناصر الرمزية تحچي وحدها.
8. **نظيف:** ماكو أسهم، ولا دوائر حمرا، ولا صناديق ملونة.

## أفكاري
<!-- أفكار صاحب القناة للأغلفة، بتاريخها -->
```

- [ ] **الخطوة 3: تأكد إن القوالب بـ brand.md تشتغل بالمركّب**
```bash
python3 - <<'EOF'
import json, sys
sys.path.insert(0, "designer")
import render
bg = {"gradient": ["#06121A", "#0F3A44"], "contrast": 1.12, "saturation": 1.2, "sharpen": 1, "vignette": 0.35}
main = {"type": "text", "text": "نهاية\nالكون!", "font": "Baloo", "x": 0.27, "y": 0.47, "size": 0.2,
        "line_spacing": 0.62, "line_colors": ["#FFFFFF", ["#FFC400", "#FF6A00"]],
        "stroke": {"color": "#0B0B0F", "width": 0.1}, "shadow": True, "rotate": 5}
paper = {"type": "text", "text": "إحنا مش\nلوحدنا!", "font": "Rubik", "x": 0.27, "y": 0.6, "size": 0.13,
         "line_spacing": 0.9, "color": "#141414", "box": {"style": "paper", "pad": 0.3}, "rotate": -4}
for layer in (main, paper):
    img, w = render.render({"background": bg, "layers": [layer]}, ".")
    print(img.size, w)
EOF
```
المتوقع: سطرين `(1280, 720) []`

- [ ] **الخطوة 4: Commit**
```bash
git add studio/brand.md studio/style-references.md
git commit -m "Add channel visual identity and style reference library"
```

---

### المهمة 6: سكيل المصمم وموظف الاستوديو

**الملفات:**
- إنشاء:
  - `.claude/skills/designer/SKILL.md`
  - `.claude/agents/designer.md`
  - `designer/canva.json`
  - `designer/faces.json`

**الواجهات:**
- تستخدم:
  - أوامر المهام 2-4: `render.py` و`sheet.py` و`cutout.py`.
  - ملفات المهمة 5.
  - أدوات Canva: `generate-image` و`get-generate-image-job` و`read-design` و`edit-design` و`export-design` و`create-upload-url` و`move-item-to-folder` و`create-folder` و`search-folders` و`list-folder-items` و`create-design` و`resize-design`.
- تنتج:
  - `designer/canva.json` بصيغة:
    ```
    {"carriers": {"youtube"|"reels"|"photo": {"design_id", "slots": ["<locator لكل صفحة>"], "export_width"} | null},
     "folders": {"faces"|"references"|"finals": "<folder_id>" | null}}
    ```
  - `designer/faces.json` بصيغة:
    ```
    {"<name>": {"canva_id", "expression", "looks"}}
    ```

- [ ] **الخطوة 1: اكتب** `.claude/skills/designer/SKILL.md`
````markdown
---
name: designer
description: المصمم الخاص بالقناة. يصمم أغلفة يوتيوب (1280×720) وأغلفة ريلز وتيك توك وشورتس (1080×1920) من فكرة أو حلقة، بهوية القناة وذوق صاحبها. يولّد الصور بـ Canva ويكتب العربي بالكود. استخدمه لما صاحب القناة يگول "صمملي غلاف"، "سوّيلي صورة مصغرة"، "ثمبنيل"، "كفر ريلز"، "غلاف تيك توك"، "غلاف للحلقة"، أو يرسل صور أغلفة تعجبه أو روابطها، أو يگول "ضفت صوري".
---

# المصمم

انت **مصمم أغلفة محترف** لقناة يوتيوب عربية. شغلك تربح النقرة بثانية وحدة، على شاشة موبايل صغيرة، بدون ما تكذب على المشاهد.

**القاعدة الذهبية:** الذكاء الاصطناعي يولّد الصور بس. **الكتابة العربية يكتبها الكود** (`designer/render.py`) لأن الذكاء الاصطناعي يخربط الحروف العربية.

## الملفات
| الملف | شنو بيه |
|---|---|
| `studio/brand.md` | الألوان، والخطوط، والقوالب الجاهزة، والتكوينات، ووصفة أوامر الخلفيات |
| `studio/style-references.md` | ذوق صاحب القناة: تحليل الأغلفة الي يحبها، ووصفة الستايل، وأفكاره |
| `designer/faces.json` | صور وجهه بـ Canva: رقم كل صورة وتعبيرها واتجاه نظرته |
| `designer/canva.json` | أرقام لوحات التنزيل والمجلدات بـ Canva |
| `designer/render.py` | يركّب الغلاف من ملف طبقات JSON (شرح الصيغة بأول الملف وبالقسم 2 بالأسفل) |
| `designer/sheet.py` | ورقة مقارنة 4 أغلفة، مع صف بحجم الموبايل |
| `designer/cutout.py` | يشيل الخلفية عن صورة شخص |

## قبل كلشي (كل جلسة)
1. اقرا `studio/brand.md` و`studio/style-references.md` و`studio/channel.md`.
2. اقرا سكيلز `titles-and-thumbnails` و`thumbnail-design` بـ `.claude/skills/` وطبّق قواعدهن.
3. تأكد من المكتبات:
   ```bash
   python3 -c "import PIL, arabic_reshaper, bidi" || pip install -q -r designer/requirements.txt
   ```
4. افحص Canva بالشبكة:
   ```bash
   curl -s -o /dev/null -m 10 -w "%{http_code}" https://export-download.canva.com/
   ```
   - إذا طلع `000`، الشبكة مسدودة: اشتغل بـ **وضع Canva بس** (القسم 6)، وذكّر صاحب القناة بإعداد الشبكة (القسم 7).
   - أي رقم ثاني، يعني تمام.

## الأوامر
| صاحب القناة يگول | تسوي |
|---|---|
| "صمملي غلاف عن X" / "غلاف للحلقة" | الشغل الكامل (القسم 1) |
| يرفق صور أغلفة / يدز روابط / "هذني مراجع" | مكتبة الإلهام (القسم 4) |
| "ضفت صوري" / "حدّث صوري" | تجهيز الوجه (القسم 5) |
| "سوّيلي نسخة ريلز" | الخطوة 8 بس |
| "غيّر الكتابة" / "كبّر الوجه" / "بدّل اللون" | عدّل ملف الطبقات، وأعد التركيب، ودز النتيجة |

## 1. الشغل الكامل

### الخطوة 1: المدخل ومجلد الشغل
- **إذا حلقة:** المجلد `episodes/<الحلقة>/design/`.
  - اقرا `01b-angle.md` و`05-package.md`: العنوان الموصى به وأفكار الصورة المصغرة.
- **إذا فكرة بس:** المجلد `designs/YYYY-MM-DD-<slug-انگليزي-قصير>/`.
  - إذا ماكو عنوان، اكتب عنوان مؤقت. الغلاف لازم يكمّل العنوان، فلازم تعرفه.
- **إذا صاحب القناة انطى فكرة للغلاف** (بكلامه أو بصورة مرجعية)، هي **الفكرة A**. ما تسأله إذا ما انطى.

### الخطوة 2: صور الوجه
- المشهد يحتاج **رقم صورة وجهه بـ Canva** (`canva_id` من `designer/faces.json`)، حتى تنحط بـ `imageReferences`.
  - اختار الصورة الأقرب للتعبير المطلوب.
- إذا `faces.json` فارغ:
  - المشاهد تكون بـ "a young man" بدل وجهه.
  - بالرد الأخير ذكّره يرفع صوره (القسم 5).

### الخطوة 3: 4 أفكار ← `design.md`
اكتب بالملف لكل فكرة (A B C D):
```markdown
## الفكرة A: <اسم قصير>
- **الإحساس:** <صدمة / فضول / خوف / شك…>
- **حلقة الرغبة:** "إذا دست، راح أعرف …"
- **التكوين:** <المقدم يمين واللغز يسار / متناظر بدون كتابة / المقدم وشخصية / الشباك>
- **المقدم:** <الزي، والتعبير، وشنو ماسك أو يسوي>
- **اللغز:** <العنصر الي يتوهج، ولونه>
- **ألوان المشهد:** <المسيطر + المتوهج، من جدول brand.md>
- **الكتابة:** "<سطر أبيض>" / "<سطر برتقالي>"، أو رقعة ورق، أو بدون كتابة
- **مستلهمة من:** <رقم مرجع من style-references.md وشنو أخذنا منه>
```
- الأفكار الأربعة لازم تختلف بالتكوين والزي واللغز والإحساس، مو بس باللون.
- وحدة منها على الأقل تجرب تكوين غير "المقدم يمين" (متناظر، أو شخصية، أو شباك).
- الكتابة **تكمّل العنوان وما تكرره**: العنوان يگول **شنو**، والغلاف يوصّل **الإحساس**.

### الخطوة 4: المشاهد بـ Canva (المقدم داخل المشهد)
لكل فكرة:
1. `generate-image`:
   - **الأمر:** بالإنگليزي حسب "وصفة أوامر المشهد" بـ `studio/brand.md`.
   - **وجهه:** إذا عندك صورة، `imageReferences: [{"type": "MEDIA", "id": "<canva_id>"}]`، واكتب `the man from the reference photo`.
   - **المقاس:** `aspectRatio: LANDSCAPE_16_9`.
   - **ممنوع** تطلب كتابة داخل الصورة، ولازم تحدد وين المساحة الفارغة للكتابة.
2. `get-generate-image-job` كل 10-15 ثانية لحد `SUCCESS`. النتيجة رقم صورة (media id) يبدي بـ `M`.
3. نزّلها بلوحة التنزيل `youtube` (القسم 3) إلى `<المجلد>/scene-A.png`، وشوفها بعينك:
   - **الوجه يشبهه؟** قارنه بصورته. إذا ما يشبهه، جرّب مرة ثانية بصورة وجه ثانية.
   - إذا ثاني مرة هم ما يشبهه، استخدم **المقدم مقصوص** (قالب الاحتياط بـ `brand.md`):
     - مشهد بدون شخص، والمساحة اليمنى فارغة.
     - فوقه صورته المقصوصة (القسم 5) بضو حافة بلون المشهد.
   - **كتابة أو حروف غريبة طلعت بالمشهد؟** أعد التوليد.
   - **إيدين أو أصابع مشوهة؟** أعد التوليد.

**إذا رصيد الذكاء الاصطناعي بـ Canva خلص:**
- استخدم خلفية متدرجة بألوان المشهد، مثل `{"gradient": ["#06121A", "#0F3A44"]}`، والمقدم مقصوص.
- وخبّر صاحب القناة.

**إذا شكل وجهه ما يطلع مضبوط أبداً بـ Canva:** خبّره إن محرك Google (Nano Banana) أدق بالوجوه، بس مدفوع (تقريباً 0.13 دولار للصورة). القرار إله.

### الخطوة 5: التركيب
1. اكتب `spec-A.json`… `spec-D.json` بالمجلد:
   - الخلفية: المشهد، مع اللمسة الأخيرة من قالب "المشهد الكامل".
   - الكتابة: من قالب "الكتابة الأساسية" أو "رقعة الورق".
2. شغّل:
   ```bash
   for x in A B C D; do python3 designer/render.py <المجلد>/spec-$x.json -o <المجلد>/$x.jpg; done
   python3 designer/sheet.py <المجلد>/A.jpg <المجلد>/B.jpg <المجلد>/C.jpg <المجلد>/D.jpg -o <المجلد>/grid.jpg
   ```
3. أي سطر يبدي بـ ⚠️ لازم يتصلح، إلا إذا عندك سبب واضح تكتبه بـ `design.md`.

### الخطوة 6: الفحص الذاتي
اقرا `grid.jpg` بأداة Read وشوفه بعينك. لكل غلاف:
- [ ] الكتابة تنقرا بالصف الصغير (حجم الموبايل)؟
- [ ] تعبير الوجه واضح بالصف الصغير؟
- [ ] 3 عناصر بالأكثر؟
- [ ] يبين زين على الخلفية البيضاء والداكنة؟
- [ ] الكتابة ما تكرر العنوان؟
- [ ] الحروف العربية متصلة ومرتبة صح؟

صلّح وأعد التركيب، **مرتين بالأكثر**.

### الخطوة 7: الاختيار
1. دز `grid.jpg` لصاحب القناة بأداة SendUserFile.
2. تحتها سطر لكل فكرة بالإحساس الي تلعب عليه.
3. اسأله بـ AskUserQuestion: "أي غلاف؟" والخيارات A وB وC وD.
   - إذا طلب دمج أو تعديل، سوّه وأعد الخطوات 5 و6.

### الخطوة 8: الغلاف الطولي (ريلز وتيك توك وشورتس)
1. مشهد طولي: `generate-image` بنفس أمر الفكرة المختارة ونفس صورة الوجه، بس بـ `aspectRatio: PORTRAIT_9_16` (شوف "الطولي" بـ `brand.md`). نزّله بلوحة `reels` إلى `scene-reels.png`.
   - إذا الرصيد ما يكفي، استخدم نفس مشهد يوتيوب مع `"focus": [x, 0.5]` على وجهه.
2. اكتب `spec-reels.json` بـ `"size": "reels"`. **رتّب من جديد، مو قص:**
   - الكتابة بالثلث العلوي، `y` بين 0.18 و0.35.
   - وجهه بالثلث الوسطي.
   - كلشي مهم بعيد عن آخر 15% من اليمين.
3. افحص بمناطق الأمان (ملف فحص بس، ما يتسلّم):
   ```bash
   python3 designer/render.py <المجلد>/spec-reels.json -o <المجلد>/check-reels.png --guides
   ```
   الكتابة والوجه لازم داخل الإطار الأزرق وبرا الأحمر. بعدها امسح `check-reels.png`.
4. النسخة النهائية:
   ```bash
   python3 designer/render.py <المجلد>/spec-reels.json -o <المجلد>/final-reels.jpg
   ```

### الخطوة 9: التسليم
1. انسخ الغلاف المختار: `cp <المجلد>/<X>.jpg <المجلد>/final-youtube.jpg`.
2. ارفع `final-youtube.jpg` و`final-reels.jpg` لـ Canva (القسم 3، "الرفع").
3. سجّل بـ `design.md`: الفكرة المختارة، ورقم كل مشهد بـ Canva، وأمر التوليد.
4. commit وpush لمجلد التصميم.
5. دز `final-youtube.jpg` و`final-reels.jpg` بأداة SendUserFile.
6. رد عليه باختصار:
   - وين الملفات.
   - إن الأغلفة صارت بحسابه بـ Canva، مجلد "أغلفة القناة".
   - اسأله: شنو عجبك وشنو ما عجبك؟ واكتب جوابه بقسم "تفضيلات صاحب القناة" بـ `studio/brand.md` بتاريخه.
7. **vidIQ:** `vidiq_score_thumbnail` يحتاج رقم فيديو منشور. بعد ما ينزل الفيديو، وإذا `vidiq_balance` يگول 30 أو أكثر، تگدر تقيّم الغلاف (5 رصيد).

## 2. صيغة ملف الطبقات (مختصر)
```json
{
  "size": "youtube",
  "background": {"image": "scene-A.png", "contrast": 1.12, "saturation": 1.2, "sharpen": 1,
                 "vignette": 0.35, "darken": 0, "blur": 0, "focus": [0.5, 0.5]},
  "layers": [
    {"type": "text", "text": "نهاية\nالكون!", "font": "Baloo", "x": 0.27, "y": 0.47, "size": 0.2,
     "line_spacing": 0.62, "line_colors": ["#FFFFFF", ["#FFC400", "#FF6A00"]],
     "stroke": {"color": "#0B0B0F", "width": 0.1}, "shadow": true, "rotate": 5},
    {"type": "text", "text": "إحنا مش\nلوحدنا!", "font": "Rubik", "x": 0.27, "y": 0.6, "size": 0.13,
     "color": "#141414", "box": {"style": "paper", "pad": 0.3}, "rotate": -4},
    {"type": "image", "src": "designer/faces/cut/shock-1.png", "x": 0.74, "y": 1.02, "h": 0.98,
     "anchor": "bottom-center", "flip": false, "glow": {"color": "#FF7A00", "radius": 0.025}}
  ]
}
```
- **`size`:** `youtube` أو `reels` أو `[عرض، ارتفاع]`.
- **كل الأرقام نسب:**
  - `x` و`y` من العرض والارتفاع.
  - `size` و`h` و`radius` من ارتفاع الصورة.
  - `stroke.width` و`box.pad` من حجم الخط.
- **الكتابة:**
  - `\n` تنزّل سطر جديد.
  - `color` لون واحد، أو تدرج `[فوق، جوه]`.
  - `line_colors` لون لكل سطر.
  - `highlight` يلوّن كلمة وحدة.
  - `rotate` الميلان بالدرجات (موجب = الجهة اليمنى لفوق).
  - `box`: صندوق ملون، أو `{"style": "paper"}` لرقعة ورق.
- **الخلفية:**
  - `contrast` و`saturation` و`sharpen` اللمسة السينمائية.
  - `darken` يغمّق، و`vignette` يغمّق الأطراف (من 0 لـ 1).
- **أشكال (نادراً، ستايلنا نظيف):** `{"type": "shape", "shape": "arrow"|"circle"|"rect", ...}`، والتفاصيل بأول `designer/render.py`.
- **المسار النسبي:** ينحسب من مجلد ملف الطبقات، وبعدين من جذر الريبو.

## 3. Canva: التنزيل والرفع
**ليش لوحة تنزيل؟** `generate-image` يرجع رقم صورة بس. حتى ننزّلها لازم تنحط بتصميم ويتصدّر.

أرقام اللوحات بـ `designer/canva.json`. كل لوحة إلها `slots`، وكل slot صفحة بيها صورة تملي الصفحة:
- `youtube`: 4 صفحات 1280×720 (للمشاهد A B C D سوا).
- `reels`: صفحة وحدة 1080×1920.
- `photo`: صفحة وحدة 1080×1440، لصور الوجه.

**موافقة صاحب القناة:** طلب ينسأل قبل كل حفظ على لوحات التنزيل. فحط كل صور الجولة باللوحة، واسأله **مرة وحدة** قبل الحفظ.
**وأي تصميم ثاني إله، ما تحفظ عليه تعديل إلا بإذنه.**

**تنزيل صور (أرقام media) إلى ملفات:**
1. `read-design` على `design_id` اللوحة، مع `open_transaction: true` و`fields: ["design_content"]`، حتى تاخذ `transaction_id`.
2. لكل صورة، `edit-design` بـ `keep_open`:
   - `page_index` رقم صفحتها (1 لـ A، و2 لـ B…).
   - عملية `update_fill`: `locator_id` من `slots`، و`asset_type: "image"`، و`asset_id` رقم الصورة، و`alt_text`.
3. اسأله بـ AskUserQuestion: "أحفظ لوحة التنزيل حتى أنزّل المشاهد وأكمل الأغلفة؟".
   - حط وياه رابط كل مشهد بصيغة `https://www.canva.com/M/<رقم الصورة>` حتى يشوفها.
   - **إذا وافق:** `edit-design` بـ `finalize: "commit"`.
   - **إذا رفض:** `finalize: "cancel"`، وكمّل بـ **وضع Canva بس** (القسم 6).
4. `export-design` بـ `{"type": "png", "width": <export_width>, "pages": [أرقام الصفحات]}`. يرجع رابط لكل صفحة.
5. نزّل كل رابط:
   ```bash
   curl -sSL -o <المجلد>/scene-A.png "<رابط الصفحة 1>"
   ```

**الرفع لـ Canva:**
1. `create-upload-url`.
2. ارفع الملف:
   ```bash
   curl -sS -X POST -H "Content-Type: application/octet-stream" --data-binary @<الملف> "<uploadUrl>"
   ```
3. من الرد خذ رقم الصورة، وانقله بـ `move-item-to-folder` لمجلد `finals` بـ `designer/canva.json`.
   - إذا المجلد ماكو، سوّه بـ `create-folder` باسم "أغلفة القناة" بالجذر (`root`) وسجّل رقمه.

## 4. مكتبة الإلهام
1. لكل صورة مرجعية يرفقها صاحب القناة، شوفها بعينك.
   - إذا رابط يوتيوب، نزّل الغلاف:
     ```bash
     curl -sSL -o designer/references/<رقم>.jpg https://i.ytimg.com/vi/<VIDEO_ID>/maxresdefault.jpg
     ```
     واقراه بأداة Read.
   - إذا مجلد "مراجع الأغلفة" بـ Canva، استخدم `search-folders` و`list-folder-items`.
2. ضيف سطر لكل مرجع بجدول "المراجع" بـ `studio/style-references.md`: المصدر، والتكوين والوجه، والكتابة، والألوان والمؤثرات، وليش قوي، وشنو ناخذ منه بالضبط.
3. **بعد 5 مراجع أو أكثر:**
   - اكتب "وصفة الستايل": الأنماط الي تتكرر (مكان الوجه، حجم الكتابة، الحد، الألوان، كثافة العناصر).
   - اقترح على صاحب القناة التغييرات على `studio/brand.md`، واسأله بـ AskUserQuestion يوافق.
   - إذا وافق، عدّل `brand.md`.
   - وإذا الريبو بيه حلقة جاهزة، سوّ غلاف تجريبي وحدة بالستايل الجديد ودزه إله.
4. **صور المراجع ما تنرفع للريبو** (`designer/references/` يتجاهله git)، لأن حقوقها لأصحابها.
5. أفكار صاحب القناة للأغلفة تنكتب بقسم "أفكاري" بتاريخها.

## 5. تجهيز الوجه
**مرة وحدة (وكل ما يضيف صور):**
1. `search-folders` بكلمة "صور الأغلفة"، وبعدها `list-folder-items` بـ `item_types: ["image"]`.
   - إذا المجلد ماكو، اطلب منه يسوي مجلد بـ Canva اسمه "صور الأغلفة" ويرفع بيه 8-10 صور.
   - الصور لازم تكون **بالطول**، وواضحة، ووجهه مضوّي ويباوع للكاميرا، وبتعابير مختلفة: مصدوم، خايف، مستغرب، جدي، شاك، يأشر.
2. لكل صورة جديدة:
   - نزّلها بلوحة `photo` إلى `designer/faces/raw/<اسم>.png`.
   - شوفها بعينك، واختار إلها اسم إنگليزي قصير حسب التعبير، مثل `shock-1` أو `curious-2`.
3. ضيفها لـ `designer/faces.json`:
   ```json
   {"shock-1": {"canva_id": "MAxxxxxxx", "expression": "مصدوم", "looks": "camera"}}
   ```
   قيم `looks`: `camera` أو `left` أو `right`، يعني وين يباوع.
4. commit لـ `designer/faces.json` بس. **الصور نفسها ما تنرفع للريبو أبداً** (`designer/faces/` يتجاهله git).

**المشهد ما يحتاج تنزيل ولا قص:** رقم الصورة بـ Canva يكفي لـ `imageReferences`.

**القص (للاحتياط بس، إذا المشهد ما حافظ على شكل وجهه):**
```bash
python3 -c "import rembg" 2>/dev/null || pip install -q "rembg[cpu]"
python3 designer/cutout.py designer/faces/raw/<اسم>.png -o designer/faces/cut/<اسم>.png
```
- أول مرة تاخذ دقيقة، لأن تنزّل نموذج القص.
- شوف الناتج بعينك. إذا القص خربان (جزء من الراس أو الإيد ناقص)، استبعد الصورة وخبّره.
- الملفات المحلية تنمسح بين الجلسات، فنزّل وقص بس الي تحتاجه.

## 6. وضع Canva بس (إذا الشبكة مسدودة)
1. `create-design` بـ `format: "YouTube Thumbnail"`، وبـ `brief` بالعربي بيه:
   - الكتابة بالضبط.
   - الألوان من `brand.md`.
   - وصف المشهد والتكوين.
2. `read-design` ثم `edit-design` حتى تضبط الكتابة (`find_and_replace_text` و`format_text`) والألوان.
   - اعرض عليه الصورة المصغرة الي يرجعها `edit-design`، **واسأله قبل الـ commit**، لأن هذا تصميمه هو.
3. للطولي: `resize-design` بـ `{"type": "custom", "width": 1080, "height": 1920}` وبعدها رتّب.
4. سلّمه روابط Canva، وگله إن الجودة الكاملة تحتاج إعداد الشبكة.

## 7. إذا الشبكة مسدودة، شنو يسوي صاحب القناة
1. claude.ai/code ← زر البيئة (Environment) ← أيقونة الإعدادات ⚙️.
2. **Network access** ← **Custom**.
3. **Allowed domains**: `*.canva.com` و`i.ytimg.com`، كل وحدة بسطر.
4. فعّل **Also include default list of common package managers**، واحفظ.
5. التغيير يشتغل من الجلسة الجاية.

## قواعد ما تنكسر
- الكتابة العربية بالكود بس. ولا مرة تطلب من Canva أو أي ذكاء اصطناعي يكتب كلام داخل الصورة.
- الكتابة سطرين و4 كلمات بالأكثر، و3 عناصر بالأكثر، وماكو حد أبيض حول المقدم.
- **نستلهم، ما ننسخ.** ممنوع تعيد غلاف شخص ثاني بنفس تفاصيله.
- ممنوع غلاف يوعد بشي مو موجود بالحلقة.
- صور وجهه وصور المراجع ما تنرفع للريبو.
- كل رسالة لصاحب القناة بالعراقي وبدون إنگليزي. أسماء الملفات تبقى مثل ما هي.
````

- [ ] **الخطوة 2: اكتب** `.claude/agents/designer.md`
```markdown
---
name: designer
description: مصمم الأغلفة بالاستوديو. يطلع 4 أفكار أغلفة يوتيوب مختلفة للحلقة ويركّبها (Canva للصور والكود للكتابة العربية) لحد ورقة المقارنة. استخدمه بعد ما يخلص مسؤول التغليف (packager). الاختيار والغلاف الطولي يكملهم مدير الإنتاج ويا صاحب القناة.
---

انت **مصمم الأغلفة** بالاستوديو.

## شغلك
1. اقرا `.claude/skills/designer/SKILL.md` واشتغل حسب تعليماته بالضبط.
2. نفّذ **الخطوات 1 لـ 6 بس** من "الشغل الكامل":
   - المدخل.
   - الوجه.
   - الأفكار الأربعة.
   - الخلفيات.
   - التركيب.
   - الفحص الذاتي.
3. **لا تسأل صاحب القناة ولا تدزله ملفات.** انت ما تگدر توصله. مدير الإنتاج يسوي الاختيار والغلاف الطولي بعدك.

## المدخل
مسار مجلد الحلقة الكامل. مجلد شغلك: `<مجلد الحلقة>/design/`.

## الرد الأخير (لمدير الإنتاج)
- مسار `grid.jpg`.
- سطر لكل فكرة: الإحساس، والكتابة، والتكوين.
- أي تحذير ⚠️ بقى وليش.
- إذا الشبكة مسدودة أو ماكو صور وجه أو رصيد Canva خلص، گولها بوضوح.
```

- [ ] **الخطوة 3: ملفات Canva والوجه**

`designer/canva.json`، ورقم لوحة يوتيوب انسوّى بالتجربة وقت كتابة الخطة:
```json
{
  "carriers": {
    "youtube": {"design_id": "DAHWNGrZtkg", "slots": ["PBHjScZYnXcZLvkV-LBrDB8jVtbR818pt"], "export_width": 1920},
    "reels": null,
    "photo": null
  },
  "folders": {"faces": null, "references": null, "finals": null}
}
```
`designer/faces.json`:
```json
{}
```

- [ ] **الخطوة 4: تأكد إن السكيل والموظف ينقرون**
```bash
python3 - <<'EOF'
import re
for p in [".claude/skills/designer/SKILL.md", ".claude/agents/designer.md"]:
    s = open(p, encoding="utf-8").read()
    m = re.match(r"---\nname: (\S+)\ndescription: (.+?)\n---\n", s, re.S)
    assert m and m.group(1) == "designer", p
    print("ok", p)
import json
json.load(open("designer/canva.json")); json.load(open("designer/faces.json")); print("ok json")
EOF
```
المتوقع: ثلاث أسطر `ok`.

- [ ] **الخطوة 5: Commit**
```bash
git add .claude/skills/designer .claude/agents/designer.md designer/canva.json designer/faces.json
git commit -m "Add designer skill and studio agent"
```

---

### المهمة 7: الربط بالاستوديو وتثبيت السكيلزات

**الملفات:**
- تعديل:
  - `.claude/skills/studio/SKILL.md`
  - `CLAUDE.md`
  - `README.md`
  - `skills.manifest`
  - `.claude/skills/.skills.lock`: يتحدث وحده بالسكربت.
- إنشاء (بالسكربت): `.claude/skills/thumbnail-design/`

- [ ] **الخطوة 1: الاستوديو** (`.claude/skills/studio/SKILL.md`)

1. بجدول "الفريق"، بعد سطر `منسق المنصات`، ضيف:
```
| مصمم الأغلفة | `designer` | `design/grid.jpg` ثم `final-youtube.jpg` و`final-reels.jpg` |
```
2. بعد قسم `### 8. التغليف والمنصات` وقبل `### 9. التسليم`، ضيف:
```markdown
### 8b. الأغلفة ← `designer`
بعد ما يخلص `packager` (المصمم يحتاج `05-package.md`):
1. شغّل `designer` على مجلد الحلقة. يرجعلك `design/grid.jpg` ووصف 4 أفكار.
2. دز `grid.jpg` لصاحب القناة بأداة SendUserFile، واسأله يختار (A أو B أو C أو D).
3. كمّل انت الخطوتين 8 و9 من `.claude/skills/designer/SKILL.md` (الغلاف الطولي والتسليم).
```
3. بقائمة الرد بالخطوة 9، بعد سطر "مسار السكربت النهائي." ضيف:
```
   - الغلاف المختار وغلاف الريلز (`design/final-youtube.jpg` و`design/final-reels.jpg`).
```
4. بجدول "أوامر سريعة"، بعد سطر `"عناوين لهذا"`، ضيف:
```
| "صمملي غلاف" / "غلاف للحلقة" / "كفر ريلز" | سكيل `designer` بس |
```

- [ ] **الخطوة 2: `CLAUDE.md`**

بجدول السكيلزات، بعد سطر `studio`، ضيف:
```
| **غلاف يوتيوب، صورة مصغرة، كفر ريلز أو تيك توك، أغلفة تعجبه** | **`designer`** (يستخدم Canva و`thumbnail-design` و`titles-and-thumbnails`) |
```

- [ ] **الخطوة 3: `README.md`**

1. بدّل "بيه **29 سكيل**" بـ "بيه **31 سكيل**".
2. ضيف القسم هذا قبل `## شلون تستخدمهن؟`:
```markdown
## 🎨 المصمم (أغلفتك)
مصمم خاص يسوي أغلفة يوتيوب وأغلفة ريلز وتيك توك وشورتس، بهوية قناتك وذوقك.

- **شلون تشغّله:**
  - اكتب «صمملي غلاف عن ...» أو «سوّيلي غلاف للحلقة».
  - يطلعلك 4 أغلفة بصورة وحدة، وانت تختار، وبعدها يسوي نسخة الريلز.
- **علّمه ذوقك:** ارفق صور أغلفة تعجبك بالمحادثة، واكتب «هذني مراجع».
- **صورك:**
  - سوّ مجلد بـ Canva اسمه «صور الأغلفة»، وارفع بيه 8-10 صور إلك بالطول وبتعابير مختلفة.
  - بعدها گول «ضفت صوري».
  - صورك ما تنرفع للريبو.
- **مرة وحدة بس، افتح الشبكة لـ Canva:**
  1. claude.ai/code ← زر البيئة ← ⚙️.
  2. Network access: **Custom**.
  3. اكتب `*.canva.com` و`i.ytimg.com`.
  4. فعّل **Also include default list**، واحفظ.
- **وين الأغلفة:** بمجلد الحلقة `design/`، وبحسابك بـ Canva بمجلد «أغلفة القناة».
```

- [ ] **الخطوة 4: `skills.manifest`**

ضيف بالآخر:
```
# تصميم الأغلفة
pottertech/claude-skills  youtube/thumbnail-design
# موقوف لحد ما ينضاف مفتاح Google (GEMINI_API_KEY) بإعدادات البيئة. شيل # من أول السطر الجاي حتى يتفعل:
# aabrole/claude-video-thumbnail-skill  skills/video-thumbnail
```

- [ ] **الخطوة 5: ثبّت وتأكد**
```bash
scripts/update-skills.sh
test -f .claude/skills/thumbnail-design/SKILL.md && echo installed
grep -c "curl\|API_KEY\|http" .claude/skills/thumbnail-design/SKILL.md
```
المتوقع:
- سطر `↑ thumbnail-design: جديد → <رقم>`.
- `installed`.
- `0`، يعني السكيل نص تعليمات بس، بدون اتصالات. انقرى كامل وقت كتابة الخطة، وهو آمن ورخصته MIT.

- [ ] **الخطوة 6: Commit**
```bash
git add .claude/skills/studio/SKILL.md CLAUDE.md README.md skills.manifest .claude/skills/.skills.lock .claude/skills/thumbnail-design
git commit -m "Wire designer into studio, docs, and install thumbnail-design skill"
```

---

### المهمة 8: لوحات التنزيل بـ Canva (مدير الإنتاج بنفسه)

**يحتاج:** موافقة صاحب القناة **مرة وحدة** قبل حفظ اللوحات الثلاث (الخطوة 4).

**الملفات:**
- تعديل: `designer/canva.json`

**الواجهات:**
- تنتج:
  - `carriers.youtube.slots`: 4 أرقام (صفحة لكل فكرة).
  - `carriers.reels` و`carriers.photo` بنفس الصيغة، وكل وحدة بيها slot واحد.

- [ ] **الخطوة 1: جهّز لوحة يوتيوب (4 صفحات)**
1. `read-design` على `DAHWNGrZtkg` بـ `open_transaction: true` و`fields: ["design_content", "page_metadata", "thumbnails"]`.
2. تأكد إن الصفحة 1 بيها عنصر واحد بحجم 1280×720 عند (0، 0).
   - إذا اكو عناصر ثانية، امسحها بـ `delete_element`.
   - إذا الحجم مو صح، صلّحه بـ `resize_element` و`position_element`.
   - تأكد إن الاسم "لوحة التنزيل - لا تحذفها"، وإلا `update_title`.
3. `edit-design` على الصفحة 1 (`keep_open`): ثلاث عمليات `add_page` بـ `width: 1280` و`height: 720`.
4. `read-design` بنفس `transaction_id` حتى تاخذ `id` الصفحات الجديدة.
5. لكل صفحة جديدة (2 و3 و4)، `edit-design` بـ `keep_open`:
   - `insert_fill` بـ `page_id` الصفحة، و`asset_type: "image"`، و`asset_id: "MAHWNA5lIBg"`.
   - `left: 0` و`top: 0` و`width: 1280` و`height: 720`.
6. `read-design` بـ `thumbnails` لكل الصفحات. تأكد إن كل صفحة الصورة تملاها كاملة. إذا وحدة ما مليانة، `crop_media` أو `resize_element`.
7. سجّل أرقام العناصر الأربعة (`locator_id`) بالترتيب.

- [ ] **الخطوة 2: لوحة الطولي (1080×1920)**
1. `resize-design` على `DAHWNGrZtkg` بـ `{"type": "custom", "width": 1080, "height": 1920}`. يرجع `design_id` جديد.
   - إذا طلع بيه 4 صفحات، امسح 2-4.
2. `read-design` عليه بـ `open_transaction: true`.
3. `edit-design` بـ `keep_open`:
   - `resize_element` للعنصر: 1080×1920، و`preserve_aspect_ratio: false`.
   - `position_element`: `top: 0` و`left: 0`.
   - `update_title`: "لوحة التنزيل الطولية - لا تحذفها".

- [ ] **الخطوة 3: لوحة صور الوجه (1080×1440)**
نفس الخطوة 2، بس بـ `{"type": "custom", "width": 1080, "height": 1440}`، والعنصر 1080×1440، والاسم "لوحة صور الوجه - لا تحذفها".

- [ ] **الخطوة 4: موافقة صاحب القناة ثم الحفظ**
1. اسأله بـ AskUserQuestion، سؤال واحد: "سوّيت 3 لوحات تنزيل بـ Canva مالتك (اسمها «لا تحذفها»). أحفظها؟".
2. **إذا وافق:** `edit-design` بـ `finalize: "commit"` للتصاميم الثلاثة.
   **إذا رفض:** `cancel` للثلاثة، واكتب بـ `canva.json` إن اللوحات مو جاهزة. المصمم يشتغل بوضع Canva بس لحد ما يوافق.

- [ ] **الخطوة 5: سجّل وجرّب التصدير**
1. `designer/canva.json`:
   ```json
   {
     "carriers": {
       "youtube": {"design_id": "DAHWNGrZtkg", "slots": ["<A>", "<B>", "<C>", "<D>"], "export_width": 1920},
       "reels": {"design_id": "<الرقم>", "slots": ["<locator>"], "export_width": 1080},
       "photo": {"design_id": "<الرقم>", "slots": ["<locator>"], "export_width": 1080}
     },
     "folders": {"faces": null, "references": null, "finals": null}
   }
   ```
2. `export-design` للوحة يوتيوب بـ `{"type": "png", "width": 1920}`.
   - المتوقع: 4 روابط تبدي بـ `https://export-download.canva.com/`.
3. نزّل واحد:
   ```bash
   curl -sS -o /tmp/carrier.png -w "%{http_code}\n" "<الرابط>"
   ```
   - إذا `200`، شوف الصورة بأداة Read.
   - إذا `000` أو `403`، الشبكة لسه مسدودة. سجّلها وكمّل.

- [ ] **الخطوة 6: Commit**
```bash
git add designer/canva.json
git commit -m "Add Canva download carriers for youtube, reels, and face photos"
```

---

### المهمة 9: التجربة الكاملة على حلقة إيفرغراند (مدير الإنتاج بنفسه)

**الملفات:**
- إنشاء: `episodes/2026-09-23-evergrande/design/`، وبيه:
  - `design.md`
  - `spec-*.json`
  - `A.jpg` لـ `D.jpg`
  - `grid.jpg`
  - `final-youtube.jpg`
  - `final-reels.jpg`

- [ ] **الخطوة 1: كل الاختبارات**

شغّل: `python3 -m unittest discover -s designer/tests -v`
المتوقع: 53 اختبار `OK`

- [ ] **الخطوة 2: افحص الشبكة**
```bash
curl -s -o /dev/null -m 10 -w "%{http_code}\n" https://export-download.canva.com/
```
- **أي رقم غير `000`:** طريق كامل. نفّذ خطوات "الشغل الكامل" بالسكيل من 1 لـ 9، بمشاهد Canva حقيقية والمقدم داخلها.
- **`000`:** طريق محدود، بخلفيات متدرجة من الكود (`gradient`) وبدون تنزيل من Canva.
  - المشاهد المولدة بالتجربة تنذكر بـ `design.md` حتى تنزل بالجلسة الجاية: `MAHWNA5lIBg` (عمارات ما مكملة) و`MAHWNc_wg-A` (سفينة أشباح).

- [ ] **الخطوة 3: نفّذ سكيل `designer` على الحلقة**
- المدخل: `episodes/2026-09-23-evergrande/`، والعنوان والزاوية من `01b-angle.md`.
- صور الوجه:
  - إذا `designer/faces.json` فارغ، المشاهد بـ "a young man".
  - إذا اكو صور، هذا أول اختبار حقيقي لـ `imageReferences`: قارن الوجه بصورته.
  - إذا ما يشبهه، جرّب الاحتياط: قص صورة بـ `cutout.py`، وشوف القص بعينك. هذا أول اختبار حقيقي لـ `rembg`.
- المتوقع:
  - `grid.jpg` بيه 4 أفكار مختلفة.
  - صفر ⚠️ بكل أوامر `render.py`.

- [ ] **الخطوة 4: الاختيار والطولي والتسليم**
1. دز `grid.jpg` لصاحب القناة، واسأله يختار.
2. سوّ `final-reels.jpg` وافحصه بـ `--guides`.
3. إذا الشبكة مفتوحة، ارفع النهائيات لـ Canva.

- [ ] **الخطوة 5: Commit وpush**
```bash
git add episodes/2026-09-23-evergrande/design designer/faces.json designer/canva.json
git commit -m "Add first designer run: Evergrande thumbnails"
git push -u origin claude/professional-image-designer-3qcfdg
```

- [ ] **الخطوة 6: الرد الأخير لصاحب القناة** (بالعراقي):
- شنو انبنى.
- شنو يسوي هو، والي ما سواه بعد:
  - إعداد الشبكة.
  - مجلد "صور الأغلفة" بـ Canva.
  - صور الأغلفة المرجعية.
- سطر "📚 تعلّم اليوم".
